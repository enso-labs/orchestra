## Codebase Patterns
- Search providers return `(results, error)` tuple pattern
- Tavily uses inline import inside the helper function
- Constants are in `backend/src/constants/__init__.py` with `UserTokenKey` enum + module-level `os.getenv()`
- Search tools are in `backend/src/tools/search.py`
- Frontend search component at `frontend/src/components/tools/SearchEngine.tsx`

---

## 2026-01-29 - US-001
Thread: (initial commit)
- Added `exa-py` to pyproject.toml dependencies
- Added `EXA_API_KEY` to `UserTokenKey` enum and module-level env var
- Added `EXA_API_KEY=` to `backend/.example.env`
- **Learnings for future iterations:**
  - Constants follow pattern: add to enum, then add module-level `os.getenv()` below
  - `.example.env` groups keys by section (Tools section)
---

## 2026-01-29 - US-002 through US-006
- Implemented `_normalize_exa_results()` mapping Exa SDK objects to canonical dict format
- Implemented `_search_with_exa()` with `asyncio.to_thread()`, try/except on import
- Reordered `web_search()` fallback: Exa -> SearXNG -> Tavily
- Added `"engines": ["tavily"]` to `_normalize_tavily_results()`
- Added `(result.engines ?? []).map(...)` defensive guard in `SearchEngine.tsx`
- Files changed: `backend/src/tools/search.py`, `frontend/src/components/tools/SearchEngine.tsx`
- **Learnings for future iterations:**
  - Exa SDK is sync-only; must wrap in `asyncio.to_thread()` for async compatibility
  - Use `getattr(item, field, default)` for Exa result objects (they're not dicts)
  - Backend tests require POSTGRES_CONNECTION_STRING env var even for unit tests (conftest imports app)
  - Frontend typechecks cleanly with `npx tsc --noEmit` from frontend dir
  - Exa `search_and_contents` returns `.results` list with `.title`, `.url`, `.highlights`, `.text`, `.score`
---
