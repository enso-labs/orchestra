{
  "project": "Orchestra",
  "branchName": "feat/734-update-scaffold",
  "description": "Update create_deep_agent scaffolding - Wire new deepagents API parameters (skills, memory, name, response_format, interrupt_on) through Orchestra's call chain, eliminate double-summarization middleware conflict, deprecate system_prompt/instructions in favor of AGENTS.md via memory parameter, and update example notebooks.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Remove duplicate compaction middleware behind feature flag",
      "description": "As a backend developer, I want to remove Orchestra's custom compaction_middleware from the default middleware stack so that only the internal SummarizationMiddleware handles message compaction, eliminating double summarization.",
      "acceptanceCriteria": [
        "init_default_middleware() conditionally excludes compaction_middleware based on USE_INTERNAL_SUMMARIZATION env var (default: 'true')",
        "When USE_INTERNAL_SUMMARIZATION=true (default), compaction_middleware is NOT in the returned middleware list",
        "When USE_INTERNAL_SUMMARIZATION=false, compaction_middleware IS in the returned middleware list (rollback path)",
        "compacting.py header comment updated to note deprecation and reference issue #734",
        "The compaction_middleware import in middleware.py remains but is guarded by the feature flag branch",
        "Typecheck passes",
        "All existing tests pass"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Files: backend/src/utils/middleware.py, backend/src/utils/compacting.py. Feature flag USE_INTERNAL_SUMMARIZATION defaults to 'true'. Read os.getenv('USE_INTERNAL_SUMMARIZATION', 'true').lower() == 'true'."
    },
    {
      "id": "US-002",
      "title": "Update middleware integration tests for new stack",
      "description": "As a backend developer, I want the middleware integration tests to reflect the updated default middleware stack (without compaction_middleware by default) so that the test suite validates the correct middleware composition.",
      "acceptanceCriteria": [
        "test_middleware_integration.py asserts init_default_middleware() returns 5 items (was 6) when USE_INTERNAL_SUMMARIZATION=true",
        "test_middleware_integration.py includes a test verifying compaction_middleware is NOT in the default stack",
        "test_middleware_integration.py includes a test verifying compaction_middleware IS in the stack when USE_INTERNAL_SUMMARIZATION=false",
        "test_compacting.py retains existing tests for the SummarizationMiddleware class itself",
        "Typecheck passes",
        "All existing tests pass"
      ],
      "priority": 2,
      "passes": true,
      "notes": "Files: backend/tests/unit/utils/test_middleware_integration.py, backend/tests/unit/utils/test_compacting.py. Depends on US-001."
    },
    {
      "id": "US-003",
      "title": "Add new fields to LLMRequest and Assistant schemas",
      "description": "As an API consumer, I want to pass skills, memory, agent_name, response_format, and interrupt_on in my API requests so that I can configure the new create_deep_agent() parameters.",
      "acceptanceCriteria": [
        "LLMRequest has new Optional fields: skills (Optional[List[str]]), memory (Optional[List[str]]), agent_name (Optional[str]), response_format (Optional[Dict[str, Any]]), interrupt_on (Optional[Dict[str, Any]]) -- all default to None",
        "Assistant has new Optional fields: skills (Optional[List[str]]), memory (Optional[List[str]]), agent_name (Optional[str]), response_format (Optional[Dict[str, Any]]), interrupt_on (Optional[Dict[str, Any]]) -- all default to None",
        "Assistant.to_llm_request() propagates the five new fields to the returned LLMRequest",
        "Existing API requests without the new fields continue to work (backward compatible)",
        "Typecheck passes",
        "All existing tests pass"
      ],
      "priority": 3,
      "passes": true,
      "notes": "Files: backend/src/schemas/entities/llm.py. Can run in parallel with US-001/US-002."
    },
    {
      "id": "US-004",
      "title": "Add schema tests for new LLMRequest and Assistant fields",
      "description": "As a backend developer, I want unit tests covering the new schema fields so that I can verify they default correctly and propagate through to_llm_request().",
      "acceptanceCriteria": [
        "Test that LLMRequest new fields all default to None when not provided",
        "Test that LLMRequest new fields are set correctly when provided",
        "Test that Assistant.to_llm_request() propagates all five new fields",
        "Typecheck passes",
        "All existing tests pass"
      ],
      "priority": 4,
      "passes": true,
      "notes": "Files: backend/tests/unit/schemas/test_llm_schemas.py (new file or add to existing). Depends on US-003."
    },
    {
      "id": "US-005",
      "title": "Wire new parameters through init_graph and Orchestra class",
      "description": "As a backend developer, I want init_graph() and Orchestra.__init__() to accept and forward skills, memory, name, response_format, and interrupt_on to create_deep_agent() so that the new API surface is available to the agent construction pipeline.",
      "acceptanceCriteria": [
        "init_graph() signature includes: skills: list[str] | None = None, memory: list[str] | None = None, name: str | None = None, response_format: Any | None = None, interrupt_on: dict[str, Any] | None = None",
        "init_graph() passes all five new parameters to create_deep_agent()",
        "Orchestra.__init__() signature includes the same five new parameters and forwards them to init_graph()",
        "construct_agent() signature includes: skills, memory, agent_name, response_format, interrupt_on",
        "construct_agent() forwards the parameters to Orchestra() (mapping agent_name to name)",
        "Typecheck passes",
        "All existing tests pass"
      ],
      "priority": 5,
      "passes": true,
      "notes": "Files: backend/src/agents/__init__.py. Depends on US-003 (schema fields must exist)."
    },
    {
      "id": "US-006",
      "title": "Wire new parameters through LLMController and stream_generator",
      "description": "As a backend developer, I want LLMController.llm_invoke(), LLMController.llm_stream(), and stream_generator() to pass the new parameters through to construct_agent() so that the full request-to-agent pipeline supports the new configuration.",
      "acceptanceCriteria": [
        "LLMController.llm_invoke() reads skills, memory, agent_name, response_format, interrupt_on from params and passes them to construct_agent()",
        "LLMController.llm_stream() reads the same fields from assistant and passes them to stream_generator()",
        "stream_generator() signature includes the five new parameters",
        "stream_generator() passes them to construct_agent()",
        "Typecheck passes",
        "All existing tests pass"
      ],
      "priority": 6,
      "passes": true,
      "notes": "Files: backend/src/controllers/llm.py, backend/src/utils/stream.py. Depends on US-005."
    },
    {
      "id": "US-007",
      "title": "Add integration test for new parameter propagation",
      "description": "As a backend developer, I want an integration test that verifies the new parameters flow from init_graph() through to create_deep_agent() so that I can confirm end-to-end wiring works.",
      "acceptanceCriteria": [
        "Test mocks create_deep_agent and verifies init_graph() passes skills, memory, name, response_format, interrupt_on through to the mock",
        "Test verifies that None defaults are passed when no new parameters are provided",
        "Typecheck passes",
        "All existing tests pass"
      ],
      "priority": 7,
      "passes": true,
      "notes": "Files: backend/tests/unit/agents/test_init_graph.py (new file). Depends on US-005."
    },
    {
      "id": "US-008",
      "title": "Deprecate compaction constants",
      "description": "As a backend developer, I want the DEFAULT_COMPACTION_* constants in backend/src/constants/llm.py marked as deprecated so that future contributors understand they are no longer actively used by the default middleware stack.",
      "acceptanceCriteria": [
        "DEFAULT_COMPACTION_TOKEN_THRESHOLD, DEFAULT_COMPACTION_RECENT_MESSAGES, and DEFAULT_COMPACTION_MODEL have deprecation comments referencing issue #734",
        "Constants are NOT removed (still used when USE_INTERNAL_SUMMARIZATION=false)",
        "Typecheck passes",
        "All existing tests pass"
      ],
      "priority": 8,
      "passes": true,
      "notes": "Files: backend/src/constants/llm.py. Independent, can run any time after US-001."
    },
    {
      "id": "US-009",
      "title": "Add markdown cells and name parameter to example notebooks",
      "description": "As a developer reading the example notebooks, I want markdown documentation cells explaining each section and the name parameter set on all agents so that I can understand the examples and get better traces.",
      "acceptanceCriteria": [
        "Each notebook has a title markdown cell with purpose, features covered, and prerequisites",
        "Each major code section is preceded by a markdown cell explaining what it demonstrates",
        "All create_deep_agent() calls include the name parameter with a descriptive kebab-case value",
        "Dead code (commented-out imports, unused provider installs) is removed",
        "deep_agent_backend.ipynb removes direct ToolRuntime import/construction (uses public API only)",
        "stream_subagent_updates.ipynb removes unused package installs (langchain-anthropic, langchain-groq, langchain-xai)",
        "Notebook outputs are cleared",
        "Typecheck passes",
        "All existing tests pass"
      ],
      "priority": 9,
      "passes": true,
      "notes": "Files: examples/agents/deep_agent_backend.ipynb, examples/agents/stream_subagent_updates.ipynb, examples/agents/RLM.ipynb. Independent, can run any time."
    },
    {
      "id": "US-010",
      "title": "Soft-deprecate system_prompt and instructions on LLMRequest and Assistant schemas",
      "description": "As a backend developer, I want the system_prompt and instructions fields on LLMRequest and Assistant to be marked as deprecated so that developers understand they should migrate to AGENTS.md files loaded via the memory parameter, aligning with the deepagents library's recommended pattern.",
      "acceptanceCriteria": [
        "system_prompt field on Assistant has a deprecation comment referencing #734 and recommending AGENTS.md via memory parameter",
        "instructions field on Assistant has a deprecation comment referencing #734 and recommending AGENTS.md via memory parameter",
        "system_prompt field on LLMRequest has a deprecation comment referencing #734 and recommending AGENTS.md via memory parameter",
        "instructions field on LLMRequest has a deprecation comment referencing #734 and recommending AGENTS.md via memory parameter",
        "validate_system_prompt_or_instructions validator has a deprecation comment noting both fields are deprecated in favor of AGENTS.md",
        "Field description values updated to note deprecation",
        "Fields are NOT removed (soft deprecation only -- still functional for backward compatibility and rollback)",
        "Typecheck passes",
        "All existing tests pass"
      ],
      "priority": 10,
      "passes": true,
      "notes": "Files: backend/src/schemas/entities/llm.py. Independent, can run any time. Comments-only change, no behavioral impact."
    },
    {
      "id": "US-011",
      "title": "Migrate system_prompt/instructions to AGENTS.md via memory parameter in agent pipeline",
      "description": "As a backend developer, I want init_graph() to stop passing system_prompt directly to create_deep_agent() and instead write the system prompt content as an AGENTS.md file to the backend filesystem and pass it via the memory parameter, aligning with the deepagents library's recommended pattern where agent instructions are provided through AGENTS.md files.",
      "acceptanceCriteria": [
        "init_graph() conditionally controls how system_prompt reaches create_deep_agent() based on USE_AGENTS_MD_INSTRUCTIONS env var (default: 'true')",
        "When USE_AGENTS_MD_INSTRUCTIONS=true: system prompt content is written to backend filesystem as AGENTS.md file",
        "When USE_AGENTS_MD_INSTRUCTIONS=true: AGENTS.md file path is prepended to the memory list",
        "When USE_AGENTS_MD_INSTRUCTIONS=true: system_prompt=None is passed to create_deep_agent()",
        "When USE_AGENTS_MD_INSTRUCTIONS=true: user-provided memory paths are preserved alongside generated AGENTS.md path",
        "When USE_AGENTS_MD_INSTRUCTIONS=false: system_prompt is passed directly to create_deep_agent() as before (rollback)",
        "When USE_AGENTS_MD_INSTRUCTIONS=false: memory is passed through unchanged",
        "Typecheck passes",
        "All existing tests pass"
      ],
      "priority": 11,
      "passes": true,
      "notes": "Files: backend/src/agents/__init__.py. Depends on US-005/US-006 (memory parameter must be wired through). Feature flag USE_AGENTS_MD_INSTRUCTIONS defaults to 'true'."
    },
    {
      "id": "US-012",
      "title": "Deprecate init_system_prompt() and update construct_agent() for AGENTS.md migration",
      "description": "As a backend developer, I want construct_agent() to support the AGENTS.md-first instruction pattern by passing instructions content through the memory parameter rather than merging it into system_prompt via init_system_prompt(), and I want init_system_prompt() marked as deprecated.",
      "acceptanceCriteria": [
        "When USE_AGENTS_MD_INSTRUCTIONS=true, construct_agent() passes instructions content to Orchestra/init_graph() via AGENTS.md mechanism rather than calling init_system_prompt()",
        "When USE_AGENTS_MD_INSTRUCTIONS=false, construct_agent() continues using init_system_prompt() to merge instructions into system_prompt (rollback path)",
        "init_system_prompt() in format.py has a deprecation comment noting it is only used when USE_AGENTS_MD_INSTRUCTIONS=false, referencing #734",
        "Metadata (timezone, language, UTC time) previously injected by init_system_prompt() is preserved in AGENTS.md content or handled through alternative mechanism",
        "Subagent system prompts are also migrated to the AGENTS.md pattern when the flag is enabled",
        "Typecheck passes",
        "All existing tests pass"
      ],
      "priority": 12,
      "passes": true,
      "notes": "Files: backend/src/agents/__init__.py, backend/src/utils/format.py. Depends on US-011."
    },
    {
      "id": "US-013",
      "title": "Add tests for system_prompt to AGENTS.md migration",
      "description": "As a backend developer, I want tests verifying that the system_prompt to AGENTS.md migration works correctly with both feature flag states, ensuring no regressions in either mode.",
      "acceptanceCriteria": [
        "Test that when USE_AGENTS_MD_INSTRUCTIONS=true, init_graph() does NOT pass system_prompt to create_deep_agent()",
        "Test that when USE_AGENTS_MD_INSTRUCTIONS=true, system_prompt content is written to backend as AGENTS.md and file path is in memory list",
        "Test that when USE_AGENTS_MD_INSTRUCTIONS=false, init_graph() passes system_prompt to create_deep_agent() as before",
        "Test that user-provided memory paths are preserved alongside generated AGENTS.md path",
        "Test that None system_prompt does not generate an AGENTS.md file",
        "Typecheck passes",
        "All existing tests pass"
      ],
      "priority": 13,
      "passes": true,
      "notes": "Files: backend/tests/unit/agents/test_init_graph.py. Depends on US-011."
    },
    {
      "id": "US-014",
      "title": "Frontend QA validation of backend changes using agent-browser",
      "description": "As a QA engineer, I want to validate that the backend changes from US-001 through US-009 do not break the frontend application by performing end-to-end browser testing using the agent-browser CLI, ensuring login, assistant management, and chat functionality all work correctly.",
      "acceptanceCriteria": [
        "Dev servers running from correct worktree (frontend on 5173 via 'npm run dev:claude' or 'npx vite --host 0.0.0.0 --port 5173', backend on 8000)",
        "Login works: navigate to http://localhost:5173, login with admin@example.com / test1234, verify redirect to main app",
        "Assistant list loads: navigate to agents/assistants page, verify at least one assistant listed without errors",
        "Assistant creation works: create a new assistant with name, description, model, and tools -- verify it saves and appears in list",
        "Chat works end-to-end: open a chat thread with an assistant, send a message, verify streamed response received without errors",
        "Console is clean: run 'agent-browser console' and 'agent-browser errors' -- no JavaScript errors or failed API requests related to schema changes",
        "Screenshot evidence: capture screenshots of (1) logged-in dashboard, (2) assistant list, (3) successful chat response -- save to .ralph/qa-screenshots/",
        "No regressions from middleware changes: chat response completes successfully, confirming compaction middleware removal does not break streaming"
      ],
      "priority": 14,
      "passes": true,
      "notes": "QA-only story, no code changes. Uses agent-browser CLI. Requires both frontend and backend dev servers running. Default login: admin@example.com / test1234. Depends on US-001 through US-009 being complete. See skills/agent-browser/SKILL.md for CLI reference."
    },
    {
      "id": "US-015",
      "title": "Backend API backward compatibility validation",
      "description": "As a QA engineer, I want to validate that the backend API accepts requests both with and without the new optional fields (skills, memory, agent_name, response_format, interrupt_on) by making curl requests against the running backend, confirming backward compatibility.",
      "acceptanceCriteria": [
        "Backend is running on port 8000 from the correct worktree",
        "Login request succeeds: curl POST to /api/auth/login with admin@example.com / test1234 returns a token",
        "Invoke without new fields works: curl POST to /api/v0/llm/invoke with only input.messages succeeds (backward compatible)",
        "Invoke with new fields works: curl POST to /api/v0/llm/invoke including skills, memory, and agent_name fields returns a valid response (no 422 validation error)",
        "OpenAPI schema includes new fields: curl GET /openapi.json contains new optional fields in LLMRequest schema definition",
        "Log evidence: append curl commands and response summaries to progress.txt"
      ],
      "priority": 15,
      "passes": true,
      "notes": "QA-only story, no code changes. Uses curl for API validation. Requires backend running on port 8000. Default login: admin@example.com / test1234."
    }
  ]
}
