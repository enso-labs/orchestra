# Ralph Progress Log
Started: Thu Feb 13 2026

## Codebase Patterns
- Backend stack: Python 3.12+, uv, FastAPI, LangChain
- Three entry points for backend: llm.py (invoke), stream.py (stream), tasks.py (worker)
- Tests: `cd backend && make test` (requires ENV_FILE=$HOME/.env/orchestra/.env.backend)
- Frontend tests: `cd frontend && npm run test`
- Commit with `-s` flag for sign-off per repo guidelines
- Daytona helpers in `backend/src/agents/daytona.py`
- Worker tasks: `backend/src/workers/tasks.py` (NOT `utils/tasks.py`)
- All 3 execution paths now use `resolve_sandbox_backend(runtime)` with no routes param
- `resolve_sandbox_backend()` is at line 267 of `backend/src/agents/__init__.py`
- Callers: llm.py:122 (invoke), stream.py:224 (stream), tasks.py:237 (worker)
- Test file: `backend/tests/unit/agents/test_daytona.py` has TestResolveSandboxBackend (5 tests) and TestCreateDaytonaBackend (4 tests)
- Test patches: `src.agents.create_daytona_backend` and `src.agents.daytona.validate_daytona_execute_capability`
- `make test` uses `ENV_FILE=$HOME/.env/orchestra/.env.backend`
- Ruff formatter converts `with patch(...), patch(...):` to parenthesized syntax
- `CompositeBackend.__init__()` requires `routes` as a positional arg — pass `routes={}` for empty routes
- After removing StoreBackend routes, `resolve_sandbox_backend()` takes only `runtime` param

---

## 2026-02-13 - US-009
- Removed `routes` keyword parameter from `resolve_sandbox_backend()` signature
- Simplified Daytona success path: `CompositeBackend(default=daytona_backend, routes={})`
- Simplified fallback path: `CompositeBackend(default=StateBackend(runtime), routes={})`
- Removed StoreBackend import and `store_backend = StoreBackend(runtime)` from all 3 callers:
  - `backend/src/controllers/llm.py`
  - `backend/src/utils/stream.py`
  - `backend/src/workers/tasks.py`
- Removed identical `routes = { f"/users/{user_id}/memories/": ..., f"/users/{user_id}/config/": ... }` dict from all 3 callers
- All 3 callers now call `resolve_sandbox_backend(runtime)` with no routes kwarg
- Files changed: `backend/src/agents/__init__.py`, `backend/src/controllers/llm.py`, `backend/src/utils/stream.py`, `backend/src/workers/tasks.py`
- **Learnings for future iterations:**
  - `CompositeBackend` requires `routes` dict as a required positional argument — cannot omit it, must pass `routes={}` for empty
  - Integration test `test_invoke_accepts_generate_files_flag` exercises the full LLM invoke path including `resolve_sandbox_backend`, so changes there are caught by integration tests
  - 7 unit tests in `TestResolveSandboxBackend` still fail because they pass `routes=` — to be fixed in US-010
---

## 2026-02-13 - US-010
- Updated all 5 remaining `TestResolveSandboxBackend` tests to call `resolve_sandbox_backend(mock_runtime)` without `routes=` param
- Removed `mock_routes` variable from `test_daytona_success_path`
- Added `assert backend.routes == {}` / `assert result_backend.routes == {}` to all 5 tests to verify no StoreBackend routes
- Deleted `test_routes_are_passed_through_on_fallback` (tested callable route factories — no longer applicable)
- Deleted `test_non_callable_routes_used_directly_on_fallback` (tested static routes — no longer applicable)
- TestResolveSandboxBackend now has 5 tests (down from 7)
- Files changed: `backend/tests/unit/agents/test_daytona.py`
- All 386 backend tests pass, all 180 frontend tests pass
- **Learnings for future iterations:**
  - `CompositeBackend.routes` is a public attribute that can be asserted against in tests
  - When removing a function parameter, test cleanup is straightforward: remove the kwarg, delete tests that exercised the removed functionality, add assertions for the new behavior
---
