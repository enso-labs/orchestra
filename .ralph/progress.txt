# Ralph Progress Log
Started: Mon Feb  9 21:07:38 MST 2026

## Codebase Patterns
- Package lives at `packages/agent-ts/` with `type: "module"` ESM setup
- Build uses plain `tsc` with bundler moduleResolution for ESM compatibility
- `.env.example` files require `git add -f` due to parent `.gitignore` pattern matching `.env*`
- `dist/` is gitignored — build artifacts are not committed
- Use `loadConfig(customEnv)` in tests instead of mutating `process.env`
- Test files live alongside source: `src/foo.test.ts` next to `src/foo.ts`
- Logger uses `process.stderr.write()` for structured JSON — spy with `vi.spyOn(process.stderr, "write").mockReturnValue(true)` in tests
- Use `any` type for `vi.spyOn(globalThis, "fetch")` to avoid complex vitest MockInstance type issues with overloaded fetch signatures
- Backend SSE format: `data: ["eventType", payload]\n\n` — event types are `metadata`, `messages`, `values`, `error`, and `[DONE]` sentinel
- Mock fetch in tests with `new Response(readableStream, { status, headers })` — use `ReadableStream` for SSE body simulation

---

## 2026-02-09 - US-001
- What was implemented: Project scaffolding for @ruska/agent-ts package
- Files changed:
  - packages/agent-ts/package.json (name, version, type, exports, scripts, deps)
  - packages/agent-ts/tsconfig.json (strict, ES2022, ESNext module, bundler resolution)
  - packages/agent-ts/.env.example (all 7 required env vars)
  - packages/agent-ts/src/index.ts (empty barrel file)
  - packages/agent-ts/.gitignore (node_modules, dist, .env)
- **Learnings for future iterations:**
  - Parent .gitignore blocks `.env.example` — use `git add -f` to force-add
  - `dist/` should be gitignored, not committed
  - `vitest` installed as devDep but no test files yet — `vitest run` will pass with 0 tests
---

## 2026-02-09 - US-002
- What was implemented: Zod-validated config from environment variables
- Files changed:
  - packages/agent-ts/src/config.ts (loadConfig function, Config type, Zod schema)
  - packages/agent-ts/src/config.test.ts (8 tests covering valid/invalid/defaults/coercion)
- **Learnings for future iterations:**
  - `loadConfig()` accepts an optional `env` parameter for testability — pass custom env maps in tests instead of mutating `process.env`
  - Zod `z.coerce.number()` handles string-to-number conversion for env vars like MAX_ITERATIONS
  - `dotenv.config()` is called at module import time in config.ts — tests that pass custom env bypass this
  - Use `.min(1)` on required string fields to catch empty string as missing
---

## 2026-02-09 - US-003
- What was implemented: Structured JSON logger with level filtering
- Files changed:
  - packages/agent-ts/src/logger.ts (createLogger factory, Logger interface, LogLevel type)
  - packages/agent-ts/src/logger.test.ts (11 tests: each level, suppression, metadata, JSON format, defaults)
- **Learnings for future iterations:**
  - Use `process.stderr.write()` not `console.error()` for structured JSON output — avoids console formatting
  - Spy on `process.stderr.write` with `vi.spyOn(process.stderr, "write").mockReturnValue(true)` in tests
  - Use `any` type for stderr spy to avoid complex vitest MockInstance type issues with overloaded write signatures
  - Logger has zero external dependencies — pure Node.js `process.stderr.write`
  - Export both `Logger` interface and `LogLevel` type for downstream consumers
---

## 2026-02-09 - US-004
- What was implemented: Zod schemas for ResearchResult, AgentState, and tool inputs
- Files changed:
  - packages/agent-ts/src/schemas.ts (ResearchResult, AgentState, WebSearchInput, FileWriterInput, HumanContactInput, NoteTakerInput schemas + types)
  - packages/agent-ts/src/schemas.test.ts (30 tests: valid/invalid parse, edge cases, boundary values, missing fields)
  - packages/agent-ts/src/index.ts (updated barrel exports for config, logger, and all schemas)
- **Learnings for future iterations:**
  - Use `z.infer<typeof Schema>` with same name for schema + type (TypeScript allows value/type with same name from Zod)
  - Use `.min(1)` on string fields that should not be empty (title, summary, query, filename, question, note)
  - Confidence uses `.min(0).max(1)` for 0-1 range validation
  - AgentState iteration uses `.int().nonnegative()` to reject negative and fractional values
  - Barrel file now exports all public API from config, logger, and schemas modules
---

## 2026-02-09 - US-005
- What was implemented: SSE stream client with async generator for Ruska /api/llm/stream endpoint
- Files changed:
  - packages/agent-ts/src/stream-client.ts (streamChat async generator, StreamEvent union type, StreamConnectionError class, SSE parser)
  - packages/agent-ts/src/stream-client.test.ts (16 tests: event parsing for metadata/messages/values/error/done, full session, keep-alive skip, malformed data skip, request headers/body, HTTP errors 401/500/400, network failure)
  - packages/agent-ts/src/index.ts (added streamChat, StreamConnectionError, StreamEvent, StreamChatRequest exports)
- **Learnings for future iterations:**
  - Backend SSE format is `data: ["eventType", payload]\n\n` — must parse JSON array with event type as first element
  - Use `any` type for `vi.spyOn(globalThis, "fetch")` — overloaded fetch signature causes complex MockInstance type incompatibilities
  - SSE `[DONE]` sentinel is not valid JSON — handle before `JSON.parse`
  - Use `ReadableStream<Uint8Array>` + `TextEncoder` to simulate SSE body in tests
  - `response.body.getReader()` + `TextDecoder({ stream: true })` handles chunked SSE data correctly
  - Backend sends `: keep-alive\n\n` comments — skip lines starting with `:`
---
