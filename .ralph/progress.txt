# Ralph Progress: Extensible Sandbox/Backend Selection (#769)
# Branch: feat/769-deep-agent-backend-sandbox-patterns
# Started: 2026-02-13

## Codebase Patterns
- `make format` (ruff) must be run from `backend/` directory, not project root
- Schema entities are exported through `backend/src/schemas/entities/__init__.py` - add new types there
- Settings patterns: UserSettings (entity), UserSettingsResponse (API response), Update*Request (request body)
- Existing settings use Optional[str] for nullable fields (not the enum type directly)
- Ruff format may reformat test files with long lines - include those in commits
- test_worker_model_resolution.py has 5 pre-existing failures (src.agents.init_backend missing) — ignore these
- Repo tests use @patch decorators on the class; each test method gets injected positional args (_dec, _enc)
- resolve_sandbox_backend(runtime, sandbox_type=None) dispatches via _SANDBOX_FACTORIES; callers pass sandbox_type from user settings
- Frontend settings service pattern: type alias for enum, add field to response interface, add API call function mirroring updateDefaultModel
- Frontend `npm install` needed in worktrees before `npm run test` or `npm run build`

## 2026-02-13 - US-001
- What was implemented: SandboxType enum, default_sandbox fields on UserSettings and UserSettingsResponse, UpdateDefaultSandboxRequest schema
- Files changed:
  - backend/src/schemas/entities/settings.py (enum + fields + request model)
  - backend/src/schemas/entities/__init__.py (exports)
  - backend/tests/unit/controllers/test_llm_model_resolution.py (ruff format)
  - backend/tests/unit/workers/test_worker_model_resolution.py (ruff format)
- **Learnings for future iterations:**
  - The `make format` and `make test` commands are in `backend/Makefile`, not root Makefile
  - Ruff format reformats long lines in test files - commit those too
  - UserSettingsResponse uses simple defaults (Optional[str] = None), not Field() for basic optional fields
---

## 2026-02-13 - US-002
- What was implemented: set_default_sandbox(sandbox) repo method with SandboxType validation, plus 3 unit tests
- Files changed:
  - backend/src/repos/user_settings_repo.py (new set_default_sandbox method + SandboxType import)
  - backend/tests/unit/repos/test_user_settings_repo.py (3 new tests: set, clear, invalid)
- **Learnings for future iterations:**
  - test_worker_model_resolution.py has 5 pre-existing failures (src.agents.init_backend missing) — not caused by our changes
  - Repo test pattern: @patch decorators on class inject _dec, _enc positional args into every test method
  - set_default_sandbox mirrors set_default_model exactly, but adds enum validation before persisting
---

## 2026-02-13 - US-003
- What was implemented: PUT /settings/default-sandbox endpoint with ValueError→400 handling, all existing endpoint responses updated to include default_sandbox field, plus 3 route tests
- Files changed:
  - backend/src/routes/v0/settings.py (new endpoint + import + all responses include default_sandbox)
  - backend/tests/unit/routes/test_settings_routes.py (3 new tests: includes_default_sandbox, set_default_sandbox, invalid_sandbox_returns_400)
- **Learnings for future iterations:**
  - Route tests use settings_client fixture with InMemoryStore + mocked encryption — no env vars needed
  - The `make test` command from backend/ directory handles env setup; running `uv run pytest` directly fails due to missing POSTGRES_CONNECTION_STRING
  - Pattern for new settings endpoints: mirror update_default_model but wrap in try/except ValueError for validation
  - All settings responses should include ALL fields from UserSettingsResponse to keep API consistent
---

## 2026-02-13 - US-004
- What was implemented: Registry pattern in resolve_sandbox_backend with sandbox_type dispatch
- Files changed:
  - backend/src/agents/__init__.py (_create_daytona_backend_checked, _create_state_backend, _SANDBOX_FACTORIES dict, updated resolve_sandbox_backend signature)
  - backend/tests/unit/agents/test_daytona.py (4 new dispatch tests in TestResolveSandboxBackendDispatch class)
- **Learnings for future iterations:**
  - Existing TestResolveSandboxBackend tests still pass with the new signature since sandbox_type defaults to None
  - The dispatch pattern: _SANDBOX_FACTORIES maps type strings to factory callables; resolve_sandbox_backend checks if type is in dict, then dispatches accordingly
  - _create_daytona_backend_checked returns None (not a tuple) on failure, so callers check `if result is not None`
  - test_explicit_state_skips_daytona verifies create_daytona_backend is never called using assert_not_called()
---

## 2026-02-13 - US-005
- What was implemented: Wired sandbox_type through LLMController — _resolve_user_settings returns 3-tuple (model, api_key, default_sandbox), llm_invoke and llm_stream both pass sandbox_type to resolve_sandbox_backend/stream_generator
- Also completed US-006: added sandbox_type parameter to stream_generator() and passed it to resolve_sandbox_backend()
- Files changed:
  - backend/src/controllers/llm.py (_resolve_user_settings 3-tuple, llm_invoke sandbox_type, llm_stream sandbox_type)
  - backend/src/utils/stream.py (sandbox_type parameter + forwarding to resolve_sandbox_backend)
  - backend/tests/unit/controllers/test_llm_model_resolution.py (FakeSettings.default_sandbox, 3-tuple unpacking in all tests, new test_default_sandbox_returned_from_settings)
- **Learnings for future iterations:**
  - FakeSettings in test_llm_model_resolution.py needs all fields that _resolve_user_settings reads via getattr
  - US-005 and US-006 are tightly coupled — stream_generator must accept sandbox_type for llm_stream to pass it
  - Using getattr(settings, "default_sandbox", None) is safer than direct attribute access for forward compatibility
  - Ruff reformats multi-line tuple unpacking — let it handle formatting
---

## 2026-02-13 - US-006 & US-007
- US-006 was already implemented during US-005 iteration — marked as passes:true
- US-007: Wired sandbox_type through _execute_agent_stream in worker tasks
- Files changed:
  - backend/src/workers/tasks.py (read default_sandbox from settings, pass to resolve_sandbox_backend)
- **Learnings for future iterations:**
  - _execute_agent_stream already fetches settings via UserSettingsRepo._get_or_create() — just read default_sandbox from it
  - Use getattr(settings, "default_sandbox", None) for forward compatibility (same pattern as LLMController)
  - default_sandbox variable must be initialized before the if/else block since settings is only available inside `if user_id:`
---

## 2026-02-13 - US-008
- What was implemented: SandboxType type alias, default_sandbox field on UserSettingsResponse interface, updateDefaultSandbox() API function
- Files changed:
  - frontend/src/lib/services/userSettingsService.ts (SandboxType type, default_sandbox field, updateDefaultSandbox function)
- **Learnings for future iterations:**
  - Frontend settings service pattern: mirror updateDefaultModel for new settings — same return type, same PUT pattern
  - UserSettingsResponse is only used within userSettingsService.ts itself; consuming components import functions not types
  - `npm install` is required in worktrees before running tests/build since node_modules aren't shared
  - 2 test suites are pre-skipped (threadService, App) and 3 individual tests skipped — all pre-existing
---

## 2026-02-13 - US-009
- What was implemented: SandboxSettings.tsx frontend component with Card + Select dropdown pattern
- Files changed:
  - frontend/src/components/settings/SandboxSettings.tsx (new file — Card with Select dropdown, 3 options: Auto/Daytona/Local)
- **Learnings for future iterations:**
  - Used Radix Select (from components/ui/select.tsx) instead of the Popover+Command combo used by DefaultModelSettings — simpler for fixed option lists
  - When sandbox is "auto" (or null from API), we send null to the API; display maps null → "auto"
  - Component is standalone — doesn't need to be wired into Settings page until US-010
  - Browser verification of an unmounted component is limited to typecheck + build pass; real browser test happens in US-010 when it's wired in
---
