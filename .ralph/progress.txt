# Ralph Progress Log
Started: Sat Feb 21 15:20:37 MST 2026

## Codebase Patterns
- Default.tsx uses `jsonValue`/`fallback` pattern: compute data vs JSX element, then single return with ternary
- Pre-commit hooks run Frontend Prettier, Frontend Lint, Frontend Test automatically on commit
- Frontend quality commands: `npm run format`, `npm run lint`, `npx tsc --noEmit`, `npx vitest run <file>`
- Screenshots for PR validation: upload as GitHub PR comment attachments, never commit to repo
- SSE streaming delivers tool call args atomically (single event) — partial JSON state is sub-millisecond
- Streaming data flow: SSE → FetchStreamReader → StreamMessageHandler.toolCall() → formatMessages() → DefaultTool
- Tool call args during streaming: null (pre-args) → partial JSON string (mid-args, very brief) → parsed object (complete)
- Use `gh api repos/{owner}/{repo}/issues/comments/{id} -X PATCH -f body=...` to update PR comments programmatically

---

## 2026-02-21 - US-001
- Refactored Default.tsx to eliminate duplicate JsonView and wrapper div
- Used `jsonValue: object | null` / `fallback: JSX.Element | null` pattern to separate data resolution from rendering
- Single JsonView (line 91) and single wrapper div (line 89) in final output
- Files changed: `frontend/src/components/tools/Default.tsx`
- **Learnings for future iterations:**
  - Prettier auto-formats on save/commit — minor whitespace changes are expected
  - Distinguishing React elements from data objects is cleanly solved by separate variables rather than runtime type checks
  - The component handles 5 cases: object input, parsed JSON object string, parsed JSON primitive string, incomplete JSON (SyntaxHighlighter), null/empty (fallback text)
---

## 2026-02-21 - US-002
- Reverted .gitignore to development base branch state using `git checkout development -- .gitignore`
- Verified `git diff development -- .gitignore` returns empty
- Files changed: `.gitignore`
- **Learnings for future iterations:**
  - Use `git checkout <base-branch> -- <file>` to cleanly revert a file to its base branch state
  - Always verify with `git diff <base-branch> -- <file>` after reverting
---

## 2026-02-21 - US-003
- Deleted 3 tracked screenshot PNGs via `git rm`: 815-complete.png, 815-streaming-2.png, 815-weather-tool-input.png
- Deleted 4 untracked screenshot PNGs: 815-streaming-1.png, 815-streaming-3.png, 815-complete-scrolled.png, 815-weather-streaming-1.png
- Typecheck passes
- Files changed: `.claude/screenshots/815-*.png` (deleted)
- **Learnings for future iterations:**
  - Use `git ls-files <pattern>` to distinguish tracked vs untracked files before cleanup
  - `git rm` stages deletions automatically; no need to `git add` after
  - Screenshots for PR validation should be uploaded as GitHub PR comment attachments, not committed to repo
---

## 2026-02-21 - US-004
- Navigated to /chat with agent-browser in --headed mode, logged in as admin@example.com
- Sent multiple queries triggering web_search tool calls: weather in SF, NYC, Tokyo, London, Paris, Berlin
- Captured screenshots during streaming showing tool call with null args and "Calling web_search tool..." status — NO red error
- Investigated streaming architecture: SSE events deliver tool call args atomically, making partial JSON state sub-millisecond
- Attempted network throttling (per-byte and per-SSE-event) to slow streaming — confirmed args arrive in single SSE events
- Verified SyntaxHighlighter fallback via test suite: all 10 DefaultTool tests pass including regression test for incomplete JSON
- Posted validation evidence as PR #816 comment (https://github.com/ruska-ai/orchestra/pull/816#issuecomment-3939631988)
- Typecheck passes
- Files changed: none (browser verification only)
- **Learnings for future iterations:**
  - Tool call args in OpenAI SSE streaming arrive atomically in single events — partial JSON state is not screenshot-capturable
  - The SyntaxHighlighter fallback is a safety net verified by tests, not a visually observable state with current models
  - agent-browser `eval` can inject custom JavaScript (fetch throttles, DOM manipulation) into the page
  - `agent-browser reload` clears injected JavaScript
  - No custom assistants exist in dev env — default model uses built-in web_search/read_file tools
---

## 2026-02-21 - US-005
- Captured completed state screenshot showing JsonView with parsed JSON args for web_search tool
- JsonView renders collapsed, interactive JSON: {"query": "current weather in San Francisco", "num_results": 3}
- Posted validation evidence as PR #816 comment
- Typecheck passes
- Files changed: none (browser verification only)
- **Learnings for future iterations:**
  - Tool call args show JsonView only when loaded from history (formatMessages applied) — during streaming, args may show null until complete
  - The SF weather thread from previous iterations served as reference for completed state
---
