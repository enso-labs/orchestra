# Ralph Progress Log
Started: Sun Feb 22 13:23:33 MST 2026

## Codebase Patterns
- Frontend state: useAgent hook in frontend/src/hooks/useAgent.ts manages agent state (model, tools, subagents, mcp, a2a, prompt)
- Agent context: AgentContext wraps useAgent, consumed via useAgentContext()
- Chat sends agent state via useChat hook: agent.model, agent.tools, agent.subagents, agent.mcp, agent.a2a
- Tool selection: BaseToolMenu + ToolSelectionModal in frontend/src/components/menus/ and modals/
- Existing subagent management in useAgent: addAgentToSubagents, removeAgentFromSubagents, toggleSubagent, isAgentSelected
- Use tsc --project tsconfig.app.json for typechecking (root tsconfig has "files": [])
- Settings persistence: useToolSelection hook pattern with debounced patchDefaults() calls
- Empty string guard: Use field_validator(mode='before') + truthy check at usage site for optional string fields that may arrive as "" from frontend
- User settings repo: _DEFAULTS_FIELD_MAP maps API field names to DB column names
- Settings route: _build_response builds DefaultsResponse from UserSettings entity
- PlatformToolsPanel: Tag filter chips use radio-style (activeTag state, null = All), with useMemo for tag extraction, counts, and two-stage filtering (text search → tag filter)
- Tag counts are computed against text-search-filtered tools, so dimming reflects real availability
---

## 2026-02-22 - US-001
- Added `default_subagents: Optional[list[str]]` to UserSettings, DefaultsResponse, and PatchDefaultsRequest schemas
- Added `subagents: default_subagents` mapping to `_DEFAULTS_FIELD_MAP` in user_settings_repo.py
- Added `subagents=settings.default_subagents` to `_build_response` in settings route
- Added 3 tests: test_patch_default_subagents, test_patch_clear_subagents, test_patch_subagents_does_not_affect_other_defaults
- Updated test_get_settings_empty to assert subagents is None for fresh users
- Files changed: backend/src/schemas/entities/settings.py, backend/src/repos/user_settings_repo.py, backend/src/routes/v0/settings.py, backend/tests/unit/routes/test_settings_routes.py
- **Learnings for future iterations:**
  - Adding a new defaults field follows a 4-step pattern: schema (3 models) → repo field map → route _build_response → tests
  - The patch_defaults mechanism is generic - just add to _DEFAULTS_FIELD_MAP and the field gets handled automatically
  - All 440 tests pass with format clean
---

## 2026-02-22 - US-002
- Added `subagents: string[] | null` to DefaultsResponse and patchDefaults types in userSettingsService.ts
- Created useSubagentSelection.ts hook mirroring useToolSelection pattern: manages Set<string> of selected IDs, debounced persistSubagents with 500ms delay, getAuthToken guard, flushPersist, toggleSubagent
- Wired hook into ToolSelectionModal/index.tsx replacing direct useAgentContext() calls for toggleSubagent/isAgentSelected
- Added flushSubagentPersist() call in handleClose
- Files changed: frontend/src/lib/services/userSettingsService.ts, frontend/src/components/modals/ToolSelectionModal/hooks/useSubagentSelection.ts (new), frontend/src/components/modals/ToolSelectionModal/index.tsx
- **Learnings for future iterations:**
  - The useSubagentSelection hook pattern parallels useToolSelection but works with Agent objects (resolved from IDs via agents list)
  - setAgent callback pattern: `setAgent((prev: Agent) => ({ ...prev, subagents: resolved }))` to avoid stale closures
  - ToolSelectionModal categories gated by isAuthenticated - subagents only visible when logged in
---

## 2026-02-22 - US-003
- Modified settings loading useEffect in BaseToolMenu.tsx to extract defaults.subagents from API response
- Added pendingSubagentIds state to store IDs until agents list is available
- Added second useEffect watching [agents, pendingSubagentIds] that resolves IDs to Agent objects
- Deleted IDs silently filtered out (agents.filter with ID check)
- Deferred resolution until agents array is populated (length > 0 guard)
- Browser verification: confirmed badge showing "6" subagents on production
- Files changed: frontend/src/components/menus/BaseToolMenu.tsx
- **Learnings for future iterations:**
  - Two-phase loading pattern: store IDs first, resolve to objects in second useEffect once dependency is ready
  - Prettier hook reformats files in pre-commit - always re-stage after first commit failure
---

## 2026-02-22 - US-004
- Verification story - no code changes needed
- Confirmed subagents tab NOT visible for unauthenticated users (sidebar shows only: Platform, MCP, A2A)
- Auth guard in useSubagentSelection.ts: `if (!getAuthToken()) return;` prevents API calls for unauth users
- BaseToolMenu.tsx: `if (!getAuthToken())` returns early before calling getSettings()
- Browser verification: screenshot confirmed modal sidebar with no subagents tab for logged-out user
- No console errors related to subagent persistence
- **Learnings for future iterations:**
  - ToolSelectionModal visible categories gated by `isAuthenticated` flag in ToolSelectionModal/index.tsx
  - Three layers of auth protection: UI visibility, hook-level guard, API-level auth middleware
---

## 2026-02-22 - US-007
- Added field_validator('model', mode='before') to Assistant class in llm.py that coerces empty and whitespace-only strings to None
- Changed init_subagents guard in agents/__init__.py from `is not None` to truthy check: `if getattr(subagent, "model", None):`
- This prevents TypeError when subagents are persisted without an explicit model (empty string "" from frontend)
- Files changed: backend/src/schemas/entities/llm.py, backend/src/agents/__init__.py
- All 440 tests pass, format clean
- **Learnings for future iterations:**
  - Pydantic field_validator(mode='before') is ideal for coercing empty strings to None before other validation runs
  - Two-layer defense: validator normalizes data at schema level, guard at usage level prevents passing empty string to init_chat_model
  - The init_subagents function only sets model on the SubAgent dict when model is truthy, allowing parent model fallback
---

## 2026-02-22 - US-005
- Added tag filter chips to PlatformToolsPanel above the tool grid
- Implemented radio-style tag selection: activeTag state (null = "All"), one active at a time
- Collected unique tags from tools via useMemo, sorted alphabetically
- Two-stage filtering: searchFilteredTools (text search only) → filteredTools (+ tag filter)
- Tag counts computed against text-search-filtered tools so dimmed state reflects real availability
- Chips: "All (N)" default + one chip per tag with count, e.g. "search (3)"
- Dimmed styling (bg-secondary/50, text-muted-foreground/50) for tags with zero matches after text search
- Horizontally scrollable chip bar (overflow-x-auto, flex-nowrap) for mobile
- Files changed: frontend/src/components/modals/ToolSelectionModal/PlatformToolsPanel.tsx
- Browser verified: tag chips render correctly, filtering works, text search + tag filter combine properly, dimming works
- Typecheck and lint pass
- **Learnings for future iterations:**
  - Two-stage filtering pattern: compute text-search-filtered set first, then derive tag counts and apply tag filter on top
  - Radio-style chip UI: use activeTag state with null meaning "All", clicking same tag deselects (returns to All)
  - Chip dimming: compute counts per tag from the intermediate filtered set, not the full tools list
  - Overflow scroll for chip bar: use -mx + px trick to extend scroll area to full width while maintaining content padding
---
