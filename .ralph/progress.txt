## Codebase Patterns
- Wiki is a Docusaurus site at wiki/ (git submodule pointing to ruska-ai/wiki)
- Docs are markdown files in wiki/docs/
- Images go in wiki/static/img/
- Sidebar config in wiki/sidebars.ts
- Changes must be committed inside the wiki submodule first, then submodule ref updated in orchestra
- AGENTS.md is stored as a memory via the memories system; each memory has a `path` (default "AGENTS.md") and `prepare_memory_files()` in `backend/src/agents/__init__.py` injects enabled memories as files into agent context
- Wiki submodule starts in detached HEAD — create a feature branch before committing
- Docusaurus admonitions use `:::tip`, `:::info`, etc. for callout blocks
- Docusaurus links must use relative markdown paths (`../memories/index.md`), not slug paths (`/memories`) — slug paths cause broken link warnings at build time
- Sidebar categories can link to a doc via `link: { type: "doc", id: "agents-md/index" }` making the category label clickable
- App theme toggle has Day/Dusk/Night/System options — must click "Day" for light mode screenshots (not `agent-browser set media light`)
- Clicking model name near chat input goes to Settings page, not a dropdown — model selection is in Settings
- The combobox near the chat input is the assistant selector, not the model selector
- Tool count button next to chat input opens a dropdown with "Upload Photo", "Web Search" toggle, and "Configure Tools" — the latter opens the full Tool Selection modal
- Tool Selection modal has 4 category tabs: Platform, API, MCP, A2A — each with its own panel
- MCP Add Server form has Template dropdown (Custom, Ruska MCP, GitHub MCP), Server Name, Transport (SSE/Streamable HTTP/STDIO), URL, Header Key/Value
- MCP config is saved to user defaults on the backend (not localStorage) via `patchDefaults()`
- Docusaurus image paths for wiki/static/img/ use `/img/...` in markdown (absolute from static root)
- Bot user (im-an-ai-agent) has WRITE on ruska-ai/orchestra but only READ on ruska-ai/wiki — must fork wiki and push there
- Models live in `backend/src/schemas/entities/store.py`, extend `BaseEntity` for persistence
- Update schemas (partial updates) extend `BaseModel` directly with all fields Optional
- Repos extend `BaseRepo` in `backend/src/repos/`, use `(user_id, entity_type)` namespace
- Services are thin wrappers in `backend/src/services/`, registered in `backend/src/contexts/service.py`
- Tools use `@tool` decorator with `ToolRuntime` for user context, registered in `backend/src/tools/__init__.py`
- `_format()` in `base_repo.py` needs extending for new entity types
- `ruff` not installed by default — run `uv pip install ruff` first
- ToolRuntime pattern: `runtime.config["metadata"].get("user_id")` for user context, `runtime.store` for BaseStore
- Helper function pattern: extract `_get_*_service(runtime)` to avoid repeating user_id/store boilerplate across tools
- SearchItem has `.key`, `.value` (dict), `.score` — use raw SearchItems when you need similarity scores instead of model_validate()
- Routes use `ServiceContext(user_id=user.id, store=store)` per request; register in `v0/__init__.py` via `app.include_router(router, prefix=prefix)`
- Frontend entities: TypeScript interfaces in `frontend/src/lib/entities/` matching backend Pydantic models
- Frontend services: static class with static methods, `apiClient` for HTTP, try-catch pattern (see `projectService.ts`)
- Frontend hooks: `useState` for state, handler functions, `useEffectGet*` wrapper for initialization (see `useProject.ts`)
- Frontend contexts: typed with `ReturnType<typeof useHook>`, null default context, `useXContext()` guard hook (see `AgentContext.tsx`)
- SubAgent infrastructure: `SubAgent` TypedDict from `deepagents` with name/description/system_prompt/tools; pass to `create_deep_agent(subagents=...)` for coordinator pattern
- SubAgent dispatch: coordinator agent gets automatic `task(description, subagent_type)` tool via `SubAgentMiddleware`; each subagent is ephemeral (stateless invocation)
---

## 2026-02-20 - US-001
- Updated `wiki/docs/memories/tutorial.md`: Added Step 6 explaining how to store AGENTS.md as a memory, including "Why", "How to Do It", and "Toggling" subsections with a `:::info` callout explaining the mechanism
- Updated `wiki/docs/memories/index.md`: Added AGENTS.md Integration bullet to Overview, rewrote How It Works steps to reflect file-based injection (not `/memories.md`), added `:::tip` callout linking to the tutorial
- Files changed: `wiki/docs/memories/tutorial.md`, `wiki/docs/memories/index.md`
- Committed in wiki submodule on branch `feat/807-docs-update-memories-onboarding-mcp`
- **Learnings for future iterations:**
  - The memories system injects each memory as a separate file (not a single `/memories.md`), based on the memory's `path` field
  - `prepare_memory_files()` in `backend/src/agents/__init__.py` is the core function that bridges memories → agent files
  - Disabled memories are filtered out via `enabled` field check
  - Wiki submodule is detached HEAD by default — must create branch before committing
- Docusaurus links must use relative markdown paths (e.g., `../memories/index.md`) not shorthand slugs (e.g., `/memories`) to avoid broken link warnings
---

## 2026-02-20 - US-002
- Updated `wiki/docs/agents-md/index.md`: Added "Storing AGENTS.md via Memories" section explaining how the memories system persists AGENTS.md instructions across all conversations, with a `:::tip` callout linking to the memories tutorial
- Updated `wiki/docs/agents-md/tutorial.md`: Added "Store AGENTS.md as a Memory (Recommended)" section with quick steps and `:::info` callout comparing file attachment vs memories vs assistants approaches; updated Next Steps with memories link
- Updated `wiki/sidebars.ts`: Reorganized Core Features to nest AGENTS.md as a subcategory with its tutorial, removed AGENTS.md tutorial from separate Tutorials category
- Updated Related Documentation in both files to include cross-references to memories docs
- Fixed all new links to use relative markdown paths for Docusaurus compatibility
- Files changed: `wiki/docs/agents-md/index.md`, `wiki/docs/agents-md/tutorial.md`, `wiki/sidebars.ts`
- Committed in wiki submodule on branch `feat/807-docs-update-memories-onboarding-mcp`
- **Learnings for future iterations:**
  - Docusaurus requires relative markdown paths (e.g., `../memories/index.md`) — shorthand slug paths like `/memories` cause broken link warnings at build time
  - The sidebar supports nested categories with `link: { type: "doc", id: "..." }` to make the category label itself link to a doc
  - Pre-existing broken links exist in the wiki (sandbox, tools pages) — don't try to fix them as part of unrelated stories
---

## 2026-02-20 - US-003
- Rewrote `wiki/docs/getting-started.md` with a complete 6-step onboarding flow:
  - Step 1: Create account or sign in (landing page + login page)
  - Step 2: Explore the interface (sidebar, chat area, top bar, user menu)
  - Step 3: Configure default model in Settings
  - Step 4: Create first memory
  - Step 5: Add AGENTS.md as a memory
  - Step 6: Send first message
- Took 5 screenshots from https://chat.ruska.ai using agent-browser:
  - `wiki/static/img/getting-started/01-landing-page.png` — landing page in light mode
  - `wiki/static/img/getting-started/02-login-page.png` — login page with email/password/OAuth
  - `wiki/static/img/getting-started/03-chat-interface.png` — main chat interface after login
  - `wiki/static/img/getting-started/04-settings-page.png` — Settings page (model, sandbox, keys)
  - `wiki/static/img/getting-started/05-memories-page.png` — Memories page (empty state)
- Added cross-references to Memories tutorial, AGENTS.md docs, and Memory Tutorial Step 6
- All links use relative markdown paths for Docusaurus compatibility
- Files changed: `wiki/docs/getting-started.md`, `wiki/static/img/getting-started/*.png`
- Committed in wiki submodule on branch `feat/807-docs-update-memories-onboarding-mcp`
- **Learnings for future iterations:**
  - The app's theme toggle has Day/Dusk/Night/System options — must click "Day" for light mode screenshots (not just `agent-browser set media light`)
  - Clicking the model name (e.g., "gpt-5.2") next to the chat input navigates to the Settings page, not a dropdown
  - The combobox near the chat input is the assistant selector, not the model selector
  - The Settings page has: Default Model, Default Sandbox, AI Provider Keys sections, plus model visibility toggles by provider further down
  - Memories page is at `/memories` (direct sidebar link), not under Settings
---

## 2026-02-20 - US-004
- Rewrote `wiki/docs/tools/mcp.md` Quick Start section with 5-step Manage Tools modal walkthrough:
  - Step 1: Open Manage Tools modal via tool count button → Configure Tools
  - Step 2: Navigate to MCP tab (sidebar navigation)
  - Step 3: Add MCP server (Template, Server Name, Transport, URL, Headers)
  - Step 4: Save and Fetch Tools (configured servers list, Fetch Tools button)
  - Step 5: Use MCP tools in chat
- Replaced all 5 outdated external GitHub-hosted GIF/images with 6 local screenshots:
  - `wiki/static/img/tools/01-tool-selection-modal.png` — full Tool Selection modal (Platform tab)
  - `wiki/static/img/tools/02-mcp-empty-state.png` — MCP tab with no servers configured
  - `wiki/static/img/tools/03-mcp-add-server-form.png` — Add Server form (empty)
  - `wiki/static/img/tools/04-mcp-template-dropdown.png` — Template dropdown (Custom/Ruska MCP/GitHub MCP)
  - `wiki/static/img/tools/05-mcp-add-server-filled.png` — Add Server form filled with sample config
  - `wiki/static/img/tools/06-mcp-server-added.png` — Configured server with Fetch Tools button
- Added Supported Transports table (SSE, Streamable HTTP, STDIO)
- Added configuration persistence info callout
- Preserved API reference sections (MCP info endpoint, Create Thread, Granular Tool Selection)
- Files changed: `wiki/docs/tools/mcp.md`, `wiki/static/img/tools/*.png`
- Committed in wiki submodule on branch `feat/807-docs-update-memories-onboarding-mcp`
- **Learnings for future iterations:**
  - The tool count button (e.g., "4") next to the chat input opens a dropdown menu, not the modal directly — must click "Configure Tools" from the dropdown
  - Tool Selection modal has 4 category tabs in a left sidebar: Platform, API, MCP, A2A
  - MCP Add Server form includes Template dropdown with presets (Custom, Ruska MCP, GitHub MCP) that pre-fill fields
  - Transport options are SSE, Streamable HTTP, and STDIO
  - MCP configuration is persisted to user defaults on the backend via `patchDefaults()` — not localStorage as the old docs suggested
  - Header Value field is masked (password-style input) for security
  - Pressing Escape while a dropdown is open inside the modal closes the entire modal — be careful when automating with agent-browser
  - Docusaurus image references to `wiki/static/img/` use absolute paths from static root: `/img/tools/01-tool-selection-modal.png`
---

## 2026-02-20 - US-005
- Pushed wiki submodule commits to fork `im-an-ai-agent/wiki` on branch `feat/807-docs-update-memories-onboarding-mcp` (bot lacks write access to `ruska-ai/wiki`)
- Updated wiki submodule reference in orchestra repo with `git add wiki`
- Committed all changes (prd.json, progress.txt, wiki submodule ref) in orchestra
- Pushed orchestra commits to `origin/feat/807-docs-update-memories-onboarding-mcp`
- Final `git status` shows branch up to date with origin; only remaining unstaged change is `wiki (modified content)` from an unrelated `package-lock.json` change
- Files changed: `.ralph/prd.json`, `.ralph/progress.txt`, `wiki` (submodule ref)
- **Learnings for future iterations:**
  - The `im-an-ai-agent` bot user has WRITE permission on `ruska-ai/orchestra` but only READ on `ruska-ai/wiki` — must fork wiki to push submodule commits
  - When pushing wiki submodule changes, add fork remote: `git remote add fork https://...@github.com/im-an-ai-agent/wiki.git`
  - A human with write access to `ruska-ai/wiki` will need to push the wiki branch upstream or merge the fork PR
---

## 2026-02-20 - US-001 (feat/812-task-epic-toolkit)
- Added Epic, EpicUpdate, Task, TaskUpdate models to `backend/src/schemas/entities/store.py`
- Epic extends BaseEntity with: name (str), description (Optional[str]), status (Optional[str] default "active")
- Task extends BaseEntity with: epic_id (str), title (str), description (Optional[str]), status (Optional[str] default "todo"), assignee (Optional[str]), blockers (list[str])
- EpicUpdate and TaskUpdate are partial update schemas extending BaseModel with all fields Optional
- Files changed: `backend/src/schemas/entities/store.py`
- **Learnings for future iterations:**
  - Models extend BaseEntity for id, metadata, created_at, updated_at fields with datetime serialization
  - Update schemas extend BaseModel (not BaseEntity) since they only need the updatable fields
  - Full app import chain requires POSTGRES_CONNECTION_STRING env var — use isolated validation for model testing
  - `ruff` needs to be installed via `uv pip install ruff` before use
---

## 2026-02-20 - US-002 (feat/812-task-epic-toolkit)
- Created `backend/src/repos/epic_repo.py` extending BaseRepo with `entity_type="epics"`
- Implements full CRUD: create, get, update, search, delete methods following ProjectRepo/MemoryRepo patterns
- Namespace: `(user_id, epics)` via BaseRepo
- Updated `backend/src/repos/base_repo.py` `_format()` method to handle "epics" and "tasks" entity types
- Added Epic and Task imports to base_repo.py
- Files changed: `backend/src/repos/epic_repo.py` (new), `backend/src/repos/base_repo.py`
- **Learnings for future iterations:**
  - MemoryRepo is the cleanest CRUD pattern to follow — simpler than ProjectRepo which has composed child repos
  - `_format()` in base_repo.py must be extended for every new entity type
  - Constructor pattern: use `Optional[BaseStore] = None` with `store = store or get_store_in_memory()` (MemoryRepo style) rather than `BaseStore = get_store_in_memory()` (ProjectRepo style)
  - `update()` uses allowed_fields set to whitelist updatable fields, matching ProjectRepo pattern
---

## 2026-02-20 - US-003 (feat/812-task-epic-toolkit)
- Created `backend/src/repos/task_repo.py` extending BaseRepo with `entity_type="tasks"`
- Implements full CRUD: create, get, update, search, delete methods following EpicRepo pattern
- Added `list_by_epic(epic_id)` method that filters tasks by epic_id using SearchFilter with `{"epic_id": epic_id}` metadata filter
- Tasks store `epic_id` in metadata dict during creation for filtering support
- Namespace: `(user_id, tasks)` via BaseRepo
- `_format()` in base_repo.py was already extended for "tasks" entity type in US-002
- Files changed: `backend/src/repos/task_repo.py` (new)
- **Learnings for future iterations:**
  - TaskRepo closely mirrors EpicRepo pattern — `list_by_epic` is the only additional method
  - Metadata dict `{"epic_id": task.epic_id}` is set during `create()` to enable SearchFilter-based filtering
  - SearchFilter `filter` parameter matches against metadata fields stored in the BaseStore
  - allowed_fields for Task update: `{"title", "description", "status", "assignee", "blockers"}`
---

## 2026-02-20 - US-004 (feat/812-task-epic-toolkit)
- Created `backend/src/services/epic.py` with EpicService class
- Epic methods: create_epic, get_epic, search_epics, update_epic, delete_epic (with cascade task deletion)
- Task methods: create_task, get_task, list_tasks, update_task, delete_task
- Registered EpicService in `backend/src/contexts/service.py` ServiceContext
- delete_epic cascades: fetches all tasks via list_by_epic, deletes each, then deletes the epic (follows ProjectService.delete pattern)
- Files changed: `backend/src/services/epic.py` (new), `backend/src/contexts/service.py`
- **Learnings for future iterations:**
  - Services are thin wrappers around repos — delegate CRUD directly, add business logic only where needed (e.g., cascade delete)
  - ServiceContext registers services with `(user_id, store)` params — add new services after existing ones but before LLMService (which depends on other services)
  - EpicService composes both EpicRepo and TaskRepo to handle cross-entity operations like cascade delete
  - ProjectService.delete is the pattern to follow for cascade deletes — iterate child entities and delete them first
---

## 2026-02-20 - US-005 (feat/812-task-epic-toolkit)
- Created `backend/src/tools/epic.py` with 9 LangChain @tool decorated async functions
- Epic tools: create_epic, list_epics, update_epic, delete_epic
- Task tools: create_task, list_tasks, update_task, delete_task, assign_task
- All tools use ToolRuntime for user context following search_threads pattern
- Helper function `_get_epic_service(runtime)` extracts user_id from runtime.config and creates EpicService
- Exported EPIC_TOOLS list with all 9 tools
- Registered EPIC_TOOLS in `backend/src/tools/__init__.py` default_tools()
- Files changed: `backend/src/tools/epic.py` (new), `backend/src/tools/__init__.py`
- **Learnings for future iterations:**
  - ToolRuntime pattern: `runtime.config["metadata"].get("user_id")` for user context, `runtime.store` for BaseStore access
  - Extract a helper like `_get_epic_service(runtime)` to avoid repeating user_id/store extraction boilerplate across tools
  - `runtime` parameter must be after required positional params but before optional keyword params in tool signatures
  - Full app import chain requires POSTGRES_CONNECTION_STRING — use `ast.parse()` or `ruff check` for isolated validation
---

## 2026-02-20 - US-006 (feat/812-task-epic-toolkit)
- Created `search_tasks` tool in `backend/src/tools/epic.py` following the `search_threads` pattern exactly
- Tool searches across all user tasks by title+description semantic similarity using BaseStore vector search
- Returns structured results with: task_id, title, status, epic_id, score
- Added `search_tasks` method to `EpicService` in `backend/src/services/epic.py` that returns raw `SearchItem` objects (preserving scores)
- Added `search_tasks` to `EPIC_TOOLS` list — automatically registered in `default_tools()` via existing `*EPIC_TOOLS` spread
- No changes needed to `backend/src/tools/__init__.py` since EPIC_TOOLS is already included
- Files changed: `backend/src/tools/epic.py`, `backend/src/services/epic.py`
- **Learnings for future iterations:**
  - To return similarity scores, use raw `SearchItem` objects instead of `model_validate()` — SearchItem has `.key`, `.value` (dict), and `.score`
  - `service.search_tasks()` returns `list[SearchItem]` while other methods return model instances — this is intentional for score access
  - `search_tasks` tool constructs response dicts manually (`item.key`, `item.value.get(...)`, `item.score`) rather than using `model_dump()`
  - The search_threads pattern: extract user_id from runtime, create service, build SearchFilter, call search, format results
---

## 2026-02-20 - US-007 (feat/812-task-epic-toolkit)
- Created `backend/src/routes/v0/epic/__init__.py` with 10 REST API endpoints
- Epic endpoints on `router` (prefix `/epics`): POST /epics, POST /epics/search, GET /epics/{id}, PUT /epics/{id}, DELETE /epics/{id}
- Task endpoints on `router` (prefix `/epics`): POST /epics/{id}/tasks, GET /epics/{id}/tasks, PUT /epics/{id}/tasks/{task_id}, DELETE /epics/{id}/tasks/{task_id}
- Task search on separate `task_router` (prefix `/tasks`): POST /tasks/search — returns task_id, title, status, epic_id, score
- All endpoints require auth via `verify_credentials` dependency
- Registered both `router` (as `epic`) and `task_router` (as `task`) in `backend/src/routes/v0/__init__.py`
- Follows ProjectRoute pattern: ServiceContext created per request with user.id and store
- Files changed: `backend/src/routes/v0/epic/__init__.py` (new), `backend/src/routes/v0/__init__.py`
- **Learnings for future iterations:**
  - Routes follow the pattern: create ServiceContext from user.id + store, call service method, return serialized result
  - Use `model_dump(exclude_unset=True)` for partial update bodies to only send changed fields
  - When an endpoint needs a separate URL prefix (e.g., /tasks/search vs /epics/...), create a second router in the same module and register both
  - `ruff check` catches unused imports — remove `logger` import if not used in route handlers
  - Create task endpoint validates epic exists first with a 404 guard before creating the task
---

## 2026-02-20 - US-008 (feat/812-task-epic-toolkit)
- Created `frontend/src/lib/entities/epic.ts` with Epic, Task, and TaskSearchResult TypeScript interfaces matching backend models
- Created `frontend/src/lib/services/epicService.ts` with API client covering all epic/task endpoints (search, create, get, update, delete for epics; createTask, listTasks, updateTask, deleteTask, searchTasks for tasks)
- Created `frontend/src/hooks/useEpic.ts` with useEpic hook managing epics, selectedEpic, tasks, loading, error state and handler methods for all CRUD operations
- Created `frontend/src/context/EpicContext.tsx` with EpicProvider and useEpicContext hook following AgentContext typed pattern
- Files changed: `frontend/src/lib/entities/epic.ts` (new), `frontend/src/lib/services/epicService.ts` (new), `frontend/src/hooks/useEpic.ts` (new), `frontend/src/context/EpicContext.tsx` (new)
- **Learnings for future iterations:**
  - Frontend entities go in `frontend/src/lib/entities/` as TypeScript interfaces matching backend Pydantic models
  - Services follow ProjectService pattern: static class with static methods, BASE_URL constant, apiClient for HTTP calls, try-catch error handling
  - Hooks follow useProject pattern: useState for state, handler functions for CRUD, useEffect wrapper for initialization
  - Contexts follow AgentContext typed pattern: `ReturnType<typeof useHook>` for type, createContext with null default, custom useXContext hook with error guard
  - `npm install` is required before `tsc` can run — typescript is a devDependency not globally installed
  - Task endpoints are nested under epics: `/epics/{id}/tasks` for CRUD, `/tasks/search` for global search
- Pages follow ChatLayout pattern: `<ChatLayout>` > `<ChatNav>` > header + content sections
- New providers must be added in `main.tsx` wrapping `<AppRoutes />`
- Sidebar links: simple `<SidebarGroup>` with `<SidebarGroupLabel asChild>` wrapping a `<Link>`
- `agent-browser eval` can set localStorage tokens to bypass PrivateRoute for UI testing without a backend (key: `enso:auth:token`)
- The Edit tool has trouble with tab-indented files — use Write to rewrite the whole file when tab indentation causes match failures
---

## 2026-02-20 - US-009 (feat/812-task-epic-toolkit)
- Created `frontend/src/pages/epics/index.tsx` with full epic list page following AgentIndexPage pattern
- Shows: epic name, description, task count (fetched per-epic via `EpicService.listTasks`), status badge
- Create epic button opens `CreateEpicModal` dialog
- Delete epic with `window.confirm()` confirmation (cascades tasks on backend)
- Search filter on epic name and description
- Empty state with "Create your first epic" CTA
- Loading state with spinner
- Created `frontend/src/components/modals/CreateEpicModal.tsx` following CreateProjectModal pattern exactly
- Added "Epics" entry to sidebar in `app-sidebar.tsx` with FolderKanban icon, linking to `/epics`
- Added `/epics` route in `AppRoutes.tsx` as a `<PrivateRoute>`
- Added `EpicProvider` to `main.tsx` wrapping `<AppRoutes />`
- TypeScript typecheck passes, Prettier formatting applied
- Verified in browser via agent-browser: empty state renders, sidebar link visible, create modal opens correctly
- Files changed: `frontend/src/pages/epics/index.tsx` (new), `frontend/src/components/modals/CreateEpicModal.tsx` (new), `frontend/src/components/drawers/app-sidebar.tsx`, `frontend/src/main.tsx`, `frontend/src/routes/AppRoutes.tsx`
- **Learnings for future iterations:**
  - Pages use `ChatLayout` > `ChatNav` > header + scrollable content pattern
  - Sidebar links use `SidebarGroup` > `SidebarGroupLabel asChild` > `Link` pattern (not collapsible)
  - New providers go in `main.tsx` wrapping `<AppRoutes />`
  - Tab-indented files (frontend uses tabs) can cause Edit tool match failures — Write tool is more reliable for these
  - `agent-browser eval "localStorage.setItem(...)"` bypasses auth for UI testing when backend isn't available
  - Task counts on epic list require per-epic API calls — consider adding a batch endpoint or including counts in search response for better performance
---

## 2026-02-20 - US-010 (feat/812-task-epic-toolkit)
- Created `frontend/src/pages/epics/EpicDetailPage.tsx` with full task management
- Task cards showing: title, status badge (color-coded), assignee, blockers (with AlertTriangle icon)
- Status badge colors: todo=outline, in_progress=default/primary, done=secondary, blocked=destructive
- Create task via `CreateTaskModal` dialog (following CreateEpicModal pattern)
- Update task status via `Select` dropdown (Radix UI) with all 4 statuses
- Delete task with `window.confirm()` confirmation
- Back navigation to `/epics` with ArrowLeft button
- Epic header shows name, description, and status summary (counts by status)
- Empty state with "Create first task" CTA
- Loading state with spinner, 404 state with "Back to Epics" button
- Created `frontend/src/components/modals/CreateTaskModal.tsx` following CreateEpicModal pattern exactly
- Added `/epics/:id` route in `AppRoutes.tsx` as a `<PrivateRoute>`
- TypeScript typecheck passes, Prettier formatting applied
- Verified in browser via agent-browser: epics page renders, detail page renders (404 state for non-existent epic), back button present
- Files changed: `frontend/src/pages/epics/EpicDetailPage.tsx` (new), `frontend/src/components/modals/CreateTaskModal.tsx` (new), `frontend/src/routes/AppRoutes.tsx`
- **Learnings for future iterations:**
  - Auth token key is `enso:auth:token` (not `token`) — use `localStorage.setItem('enso:auth:token', 'test-token')` for browser testing bypass
  - EpicDetailPage manages its own local state (epic, tasks) fetched directly via EpicService, rather than relying solely on context — this allows independent data loading per page
  - Select component from Radix UI works well for inline status updates on task cards
  - The detail page refetches tasks after CreateTaskModal closes (via onClose callback) to ensure fresh data
---

## 2026-02-20 - US-011 (feat/812-task-epic-toolkit)
- Created `start_epic_execution` tool in `backend/src/tools/epic.py` that dispatches assigned tasks to subagents
- Tool flow: fetch tasks by epic → filter assigned todo tasks → group by assignee → validate max 3 per subagent → set tasks to in_progress → build SubAgent specs → create coordinator via `create_deep_agent` → dispatch → return summary
- Uses `deepagents.SubAgent` TypedDict infrastructure: each assignee becomes a SubAgent with `name`, `description`, `system_prompt` (containing task context), and `tools` ([update_task])
- Coordinator agent built with `create_deep_agent(subagents=...)` dispatches to each subagent via the automatic `task()` tool
- Each subagent receives task IDs, titles, and descriptions in its system prompt, and can call `update_task(task_id, status='done')` to mark tasks complete
- Validates max 3 tasks per subagent (returns error if exceeded)
- Added `start_epic_execution` to EPIC_TOOLS list (now 11 tools total) — automatically registered in default_tools()
- The existing `assign_task` tool already handles setting the assignee field (implemented in US-005)
- Files changed: `backend/src/tools/epic.py`
- **Learnings for future iterations:**
  - `SubAgent` is a TypedDict from `deepagents` with required fields: name, description, system_prompt, tools
  - `create_deep_agent` with `subagents` param creates a `SubAgentMiddleware` that exposes a `task(description, subagent_type)` tool to the main agent
  - Subagent tools get `ToolRuntime` injected automatically — need to pass `store` to `create_deep_agent` and `metadata.user_id` in the RunnableConfig
  - Each subagent invocation is stateless/ephemeral — only the final message is returned to the coordinator
  - Full import chain requires POSTGRES_CONNECTION_STRING — use `ast.parse()` + `ruff check` for isolated validation
  - Imports for heavy dependencies (`create_deep_agent`, `init_chat_model`) should be inside the function to avoid module-level side effects
---

## 2026-02-20 - US-012 (feat/812-task-epic-toolkit)
- Verified branch `feat/812-task-epic-toolkit` is correct and up to date
- Committed `frontend/package-lock.json` peer dependency metadata changes
- Ran `ruff check` on backend — all checks passed
- Ran `tsc --noEmit` on frontend — no errors
- Verified `git status` — working tree clean
- Pushed 21 commits to `origin/feat/812-task-epic-toolkit` for PR #813
- Updated prd.json: set US-012 `passes: true`
- All 12 user stories now have `passes: true`
- Files changed: `frontend/package-lock.json`, `.ralph/prd.json`, `.ralph/progress.txt`
- **Learnings for future iterations:**
  - package-lock.json peer dependency changes are safe to commit — they reflect `npm install` resolution differences
  - Always verify typecheck on both backend (ruff) and frontend (tsc) before final push
  - Use `git push origin <branch>` to push all local commits at once
---

## 2026-02-20 - US-008 (re-verification)
- Re-verified existing US-008 implementation — all files present and typecheck clean
- Files verified: `frontend/src/lib/entities/epic.ts`, `frontend/src/lib/services/epicService.ts`, `frontend/src/hooks/useEpic.ts`, `frontend/src/context/EpicContext.tsx`
- `npx tsc --noEmit` passes with zero errors
- Marked `passes: true` in prd.json
- **Learnings for future iterations:**
  - When prd.json is reverted to `passes: false` but code is already committed, verify typecheck and re-mark as passing
---

## 2026-02-20 - US-009 (re-verification)
- Re-verified existing US-009 implementation — epic list page, sidebar, route, modal all present
- Files verified: `frontend/src/pages/epics/index.tsx`, `frontend/src/components/modals/CreateEpicModal.tsx`, `frontend/src/components/drawers/app-sidebar.tsx`, `frontend/src/routes/AppRoutes.tsx`, `frontend/src/main.tsx`
- Typecheck clean, marked `passes: true`
---

## 2026-02-20 - US-010 (re-verification)
- Re-verified existing US-010 implementation — EpicDetailPage with task cards, status dropdown, create/delete, back nav
- Files verified: `frontend/src/pages/epics/EpicDetailPage.tsx`, `frontend/src/components/modals/CreateTaskModal.tsx`
- Route `/epics/:id` in AppRoutes.tsx confirmed
- Typecheck clean, marked `passes: true`
---
