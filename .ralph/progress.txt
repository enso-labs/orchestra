# Ralph Progress Log
Started: Thu Feb  5 17:18:57 MST 2026
---

## Codebase Patterns
- ServiceContext (backend/src/contexts/service.py) creates per-request service instances with the correct user_id and AsyncPostgresStore — always prefer service_context.memory_service over the module-level singleton
- The module-level memory_service singleton in backend/src/services/memory.py defaults to InMemoryStore (get_store_in_memory()) — only suitable for legacy add_memories_to_system()
- prepare_memory_files() should NOT mutate user_id on the passed service; ServiceContext already initialises MemoryService with the correct user_id
- When removing imports, check if other functions in the file still use them (e.g., add_memories_to_system still uses the singleton)
- MemoryRepo stores Memory objects with a 'content' key (not 'memory') — use value.get('content', ...) when parsing SearchItem values

---

## 2026-02-05 - US-008
- What was implemented: Replaced the module-level memory_service singleton with service_context.memory_service in all three entry points (stream.py, tasks.py, llm.py) so memories persisted through the Settings UI are correctly retrieved at agent runtime
- Files changed:
  - backend/src/agents/__init__.py — removed memory_svc.user_id mutation from prepare_memory_files()
  - backend/src/utils/stream.py — switched to service_context.memory_service, removed singleton import
  - backend/src/workers/tasks.py — switched to service_context.memory_service, removed singleton import
  - backend/src/controllers/llm.py — switched to self.service_context.memory_service, removed singleton import
  - backend/tests/unit/agents/test_prepare_memory_files.py — removed test_sets_user_id_on_service_before_search test
  - .ralph/prd.json — marked US-008 as passes: true
- **Learnings for future iterations:**
  - ServiceContext already initialises MemoryService with the correct user_id and store — no need to mutate user_id on the service
  - The module-level singleton uses InMemoryStore which is why searches return empty in production
  - When removing a test that validates removed behavior, also verify no other tests depend on it
---

## 2026-02-05 - US-009
- What was implemented: Fixed memory value parsing in prepare_memory_files() to use 'content' key instead of 'memory' to match MemoryRepo storage format. Updated all test mocks and added a test case with the full MemoryRepo value structure.
- Files changed:
  - backend/src/agents/__init__.py — changed value.get('memory', ...) to value.get('content', ...)
  - backend/tests/unit/agents/test_prepare_memory_files.py — updated _make_search_item to use 'content' key, added test_handles_full_memory_repo_value_structure test
  - .ralph/prd.json — marked US-009 as passes: true
- **Learnings for future iterations:**
  - MemoryRepo.create() stores Memory(id, content, metadata, created_at, updated_at) — model_dump() produces a dict with 'content' key, NOT 'memory'
  - SearchItem.value returns the dict from model_dump(), so parsing code must use value.get('content', ...) to extract the text
  - Always verify mock data structures match the actual production data by checking the Pydantic model and repo code
---
