# Ralph Progress Log
Started: Thu Feb 13 2026

## Codebase Patterns
- Backend stack: Python 3.12+, uv, FastAPI, LangChain
- Three entry points for backend: llm.py (invoke), stream.py (stream), tasks.py (worker)
- Backend init: CompositeBackend(default=StateBackend, routes=built_routes)
- Tests: `cd backend && make test` (requires ENV_FILE=$HOME/.env/orchestra/.env.backend)
- Frontend tests: `cd frontend && npm run test`
- Commit with `-s` flag for sign-off per repo guidelines
- Daytona helpers in `backend/src/agents/daytona.py`
- Settings: schema in schemas/entities/settings.py, repo in repos/user_settings_repo.py, routes in routes/v0/settings.py
- Worker tasks: `backend/src/workers/tasks.py` (NOT `utils/tasks.py`)
- After US-004: all 3 execution paths use `resolve_sandbox_backend()` — `init_backend()` only called from within `resolve_sandbox_backend()`

---

## 2026-02-13 - US-001
- Implemented `resolve_sandbox_backend(runtime, *, routes)` in `backend/src/agents/__init__.py`
- Created `backend/src/agents/daytona.py` with `validate_daytona_execute_capability()`
- Added `create_daytona_backend()` factory function (no api_key param — uses DAYTONA_API_KEY constant)
- Added Daytona conditional imports (daytona, langchain_daytona) with None fallbacks
- Added `DAYTONA_API_KEY` to UserTokenKey enum and constants
- Added `daytona>=0.140.0` and `langchain-daytona>=0.0.2` to pyproject.toml
- `init_backend()` kept for now — callers in stream.py and tasks.py still use it; will be removed in US-007
- Files changed: `backend/src/agents/__init__.py`, `backend/src/agents/daytona.py` (new), `backend/src/constants/__init__.py`, `backend/pyproject.toml`
- **Learnings for future iterations:**
  - Pyright reports `Daytona(...)` as "Object of type None cannot be called" due to the conditional import fallback to None — use `# type: ignore[misc]` on that call
  - `create_daytona_backend()` no longer takes an `api_key` parameter; it always uses `DAYTONA_API_KEY` from constants
  - The resolve function silently falls back (no logging, no messages) when Daytona is unavailable
  - Baseline pyright error count for `__init__.py` is 40 (pre-existing, not from these changes)
---

## 2026-02-13 - US-002
- Replaced `self.init_backend(params)` in `llm_invoke()` with `resolve_sandbox_backend(runtime, routes=routes)`
- Deleted `init_backend()` method from `LLMController` class
- Updated imports: added `resolve_sandbox_backend`, removed unused `CompositeBackend` and `StateBackend`
- `_resolve_user_settings()` was already returning a 2-tuple `(model, api_key)` — no changes needed
- No `sandbox_backend`, `create_daytona_backend`, `daytona_fallback_message`, or `validate_daytona_execute_capability` imports existed in llm.py
- Files changed: `backend/src/controllers/llm.py`
- **Learnings for future iterations:**
  - llm.py `_init_runtime()` creates a ToolRuntime suitable for passing to `resolve_sandbox_backend()` — reuse it
  - Baseline pyright error count for llm.py is 24 (pre-existing); after changes it's 23
  - The invoke path now creates runtime + StoreBackend routes inline, same pattern as stream.py
---

## 2026-02-13 - US-003
- Replaced `init_backend(runtime, routes=routes)` with `resolve_sandbox_backend(runtime, routes=routes)` in `stream_generator()`
- Updated import: replaced `init_backend` with `resolve_sandbox_backend` from `src.agents`
- `stream_generator()` never had a `sandbox_backend` parameter or daytona-specific imports — only the backend factory call needed changing
- `llm_stream()` in llm.py was already clean (no `sandbox_backend` arg passed to `stream_generator`)
- Files changed: `backend/src/utils/stream.py`
- **Learnings for future iterations:**
  - Baseline pyright error count for stream.py is 20 (pre-existing, not from these changes)
  - stream.py is in `utils/` not `controllers/` — the stream generator is a utility, not a controller
  - After US-003, only tasks.py still uses `init_backend()` — it's the last caller to migrate (US-004)
---

## 2026-02-13 - US-004
- Replaced `init_backend(runtime, routes=routes)` with `resolve_sandbox_backend(runtime, routes=routes)` in `_execute_agent_stream()`
- Updated import: replaced `init_backend` with `resolve_sandbox_backend` from `src.agents`
- tasks.py had no `sandbox_backend` lookup from `UserSettingsRepo`, no daytona-specific imports, and no `api_key` resolution related to sandbox — only the backend factory call needed changing
- `api_key` resolution left intact (not present in this path anyway)
- Files changed: `backend/src/workers/tasks.py`
- **Learnings for future iterations:**
  - tasks.py is in `workers/` not `utils/` — the PRD title says "tasks.py" but the path is `src/workers/tasks.py`
  - Baseline pyright error count for tasks.py is 26 (pre-existing, not from these changes)
  - After US-004, all three execution paths (llm.py invoke, stream.py stream, tasks.py worker) now use `resolve_sandbox_backend()`
  - `init_backend()` in `__init__.py` now has no direct callers except `resolve_sandbox_backend()` itself — ready for cleanup in US-007
---

## 2026-02-13 - US-005
- Verified all sandbox_backend settings infrastructure was already absent from the codebase
- No `sandbox_backend` field in UserSettings or UserSettingsResponse models
- No `UpdateSandboxBackendRequest` class in settings.py
- No `set_sandbox_backend()` method in user_settings_repo.py
- No `PUT /settings/sandbox-backend` endpoint or `_VALID_SANDBOX_BACKENDS` constant in routes/v0/settings.py
- No `sandbox_backend=...` in any UserSettingsResponse constructor
- Pyright reports 0 errors on all three settings files
- Only change: `make format` reformatted an import in tasks.py (cosmetic)
- Files changed: None (just formatting)
- **Learnings for future iterations:**
  - The sandbox_backend settings infrastructure was removed prior to this branch (or never existed in current form)
  - PRD acceptance criteria may describe removals that are already done — always verify current state before making changes
  - `make format` may reformat imports touched in previous stories — include those formatting fixes in the next commit
---

## 2026-02-13 - US-006
- Verified all sandbox_backend frontend infrastructure was already absent from the codebase
- No `SandboxBackendSettings.tsx` component exists (or test file)
- No `SandboxBackend` type, `sandbox_backend` field, or `updateSandboxBackend()` in `userSettingsService.ts`
- No `settings/index.tsx` imports `<SandboxBackendSettings />`
- No `userSettingsService.test.ts` exists
- No `sandbox_backend` or `SandboxBackend` references found anywhere in `frontend/src/`
- Frontend typecheck passes: 0 errors
- Frontend tests pass: 180 passed, 3 skipped
- Files changed: None (just PRD and progress updates)
- **Learnings for future iterations:**
  - The sandbox_backend frontend infrastructure was removed prior to this branch (or never existed in current form)
  - Same pattern as US-005 — PRD acceptance criteria describe removals that are already done
  - Settings components live in `frontend/src/components/settings/` (currently: ModelVisibility, ApiTokens, UserApiKeys, DefaultModel, Memory)
  - `userSettingsService.ts` is at `frontend/src/lib/services/userSettingsService.ts`
  - Frontend tests: 18 test files, 180 tests, run via `cd frontend && npm run test`
---

## 2026-02-13 - US-007
- Removed `init_backend()` function from `backend/src/agents/__init__.py`
- Inlined its route-building logic into the fallback path of `resolve_sandbox_backend()`
- Verified `daytona_fallback_message()` was already absent from `backend/src/agents/daytona.py`
- Verified `validate_daytona_execute_capability()` is still present and used by `resolve_sandbox_backend()`
- No orphaned imports found across any modified files
- All 373 backend tests pass
- Files changed: `backend/src/agents/__init__.py`
- **Learnings for future iterations:**
  - `init_backend()` was only used internally by `resolve_sandbox_backend()` — inlining was straightforward
  - `daytona_fallback_message()` was removed in a prior branch or never existed in this codebase
  - The pattern of PRD acceptance criteria describing already-done removals continues (US-005, US-006, US-007)
---

## 2026-02-13 - US-008
- Created `backend/tests/unit/agents/test_daytona.py` with 11 tests:
  - `TestCreateDaytonaBackend`: 4 tests (not installed, missing API key, success, exception handling)
  - `TestResolveSandboxBackend`: 7 tests (Daytona success path, fallback when not capable, fallback when not installed, sandbox cleanup exception swallowed, silent fallback no messages, callable route factories invoked, non-callable routes used directly)
- Created `backend/tests/unit/agents/test_daytona_capability.py` with 4 tests:
  - `TestValidateDaytonaExecuteCapability`: callable execute, None backend, no execute attr, non-callable execute
- No pre-existing test files for sandbox_backend/daytona existed — `test_daytona_cross_path.py` and `test_stream_daytona_backend_routing.py` were already removed in a prior branch
- No sandbox_backend references found in any existing test files — already clean
- All 388 backend tests pass (15 new + 373 existing)
- All 180 frontend tests pass (3 skipped)
- Files changed: `backend/tests/unit/agents/test_daytona.py` (new), `backend/tests/unit/agents/test_daytona_capability.py` (new)
- **Learnings for future iterations:**
  - Test files for `create_daytona_backend()` must patch `src.agents.Daytona`, `src.agents.DaytonaSandbox`, `src.agents.DaytonaConfig`, and `src.agents.DAYTONA_API_KEY` since the conditional imports resolve at module load time
  - Tests for `resolve_sandbox_backend()` should patch `src.agents.create_daytona_backend` and `src.agents.daytona.validate_daytona_execute_capability` (note: the validator is patched on the daytona sub-module, not on src.agents directly)
  - `make test` uses `ENV_FILE=$HOME/.env/orchestra/.env.backend` — tests won't run without it (conftest.py needs POSTGRES_CONNECTION_STRING)
  - Ruff formatter converts `with patch(...), patch(...):` to parenthesized `with (patch(...), patch(...)):` syntax
---
