# Ralph Progress Log
Started: Wed Feb 11 2026

## Codebase Patterns
- Backend stack: Python 3.12+, uv, FastAPI, LangChain
- Conditional imports use try/except ImportError pattern (see src/tools/code.py)
- Backend init: CompositeBackend(default=StateBackend, routes=built_routes)
- Three entry points for backend: llm.py (invoke), stream.py (stream), tasks.py (worker)
- Config uses extra="allow" so metadata.sandbox is accessible without schema changes
- Tests in backend/tests/unit/ and backend/tests/integration/

- `langchain-daytona` depends on `daytona` package but doesn't pin a minimum version; without explicit `daytona>=0.140.0`, uv resolves to old v0.18.1 which uses `daytona_sdk` import name instead of `daytona`
- Run tests from backend/ dir: `make test`
- Commit with `-s` flag for sign-off per repo guidelines
- Daytona sandbox capability checks and fallback copy are centralized in `backend/src/agents/daytona.py`; invoke/stream/worker should all call this helper to avoid drift

---

## 2026-02-11 - US-001
- What was implemented: Added `langchain-daytona>=0.0.2` and `daytona>=0.140.0` to backend/pyproject.toml dependencies
- Files changed: backend/pyproject.toml, backend/uv.lock
- **Learnings for future iterations:**
  - The PRD specified `>=0.1.0` but only `0.0.2` exists on PyPI. Had to adjust version constraint.
  - `langchain-daytona` requires `daytona` but doesn't pin a minimum version. Old `daytona` (v0.18.1) installs as `daytona_sdk` not `daytona`. Must explicitly add `daytona>=0.140.0` to ensure correct module name.
  - `uv sync` resolves from the worktree's backend/ directory
  - All 373 existing tests pass with the new dependencies
---

## 2026-02-11 - US-002
- What was implemented: Added DAYTONA_API_KEY to src/constants and UserTokenKey enum. Added conditional import for Daytona/DaytonaSandbox and create_daytona_backend() factory in src/agents/__init__.py.
- Files changed: backend/src/constants/__init__.py, backend/src/agents/__init__.py
- **Learnings for future iterations:**
  - DaytonaSandbox(sandbox=sandbox) takes a daytona.Sandbox, extends deepagents.backends.sandbox.BaseSandbox which implements BackendProtocol
  - Daytona client: Daytona(DaytonaConfig(api_key=key)) then client.create() returns a Sandbox
  - Sandbox has .stop() method for cleanup
  - DAYTONA_API_KEY was not in constants yet (PRD assumed #750 added it) - had to add it ourselves
---

## 2026-02-11 - US-003
- What was implemented: Wired Daytona backend into LLMController.llm_invoke() with sandbox type check, fallback SystemMessage, and cleanup in finally block.
- Files changed: backend/src/controllers/llm.py
- **Learnings for future iterations:**
  - Access sandbox config via getattr(params.metadata, 'sandbox', None) for Pydantic models with extra='allow'
  - Store routes must be preserved when using Daytona backend - rebuild them and pass to CompositeBackend
---

## 2026-02-11 - US-004
- What was implemented: Wired Daytona backend into stream_generator() with sandbox type check, SSE fallback message, and cleanup in finally block.
- Files changed: backend/src/utils/stream.py
- **Learnings for future iterations:**
  - In stream.py, config['metadata'] is a dict (not Pydantic), so use .get('sandbox')
  - SSE fallback uses ujson.dumps(("system", "message")) format
---

## 2026-02-11 - US-005
- What was implemented: Wired Daytona backend into _execute_agent_stream() in tasks.py with sandbox check, Redis stream fallback message, and cleanup in finally block.
- Files changed: backend/src/workers/tasks.py
- **Learnings for future iterations:**
  - tasks.py uses late imports inside function body (imports at function top, not module top)
  - config.get("metadata", {}).get("sandbox") for dict-based config access
  - Wrapped agent execution in try/finally for sandbox cleanup while preserving existing error handling
---

## 2026-02-11 - US-006
- What was implemented: Added 8 unit tests for create_daytona_backend() in backend/tests/unit/services/test_daytona.py.
- Files changed: backend/tests/unit/services/test_daytona.py
- **Learnings for future iterations:**
  - Tests need env file loaded (make test handles this): `set -a && . $ENV_FILE && set +a`
  - Use unittest.mock.patch on "src.agents.DaytonaSandbox" etc. for mocking conditional imports
  - 381 total tests pass (373 original + 8 new)
---

## 2026-02-13 06:44 UTC - US-001: Validate Daytona backend execute-capability contract and centralize fallback messaging
- Implemented shared Daytona capability helper (`validate_daytona_execute_capability`) and centralized fallback text builder (`daytona_fallback_message`) with deterministic reason constants.
- Updated invoke/stream/worker Daytona routing to use shared helper for execute-capability checks and shared fallback messaging while preserving existing StoreBackend route construction.
- Files changed: backend/src/agents/daytona.py, backend/src/controllers/llm.py, backend/src/utils/stream.py, backend/src/workers/tasks.py, backend/tests/unit/services/test_daytona_capability.py
- **Learnings:** Shared helper removes message drift across execution paths and keeps future Daytona protocol checks in one place.
---

## 2026-02-13 07:37 UTC - US-002: Apply execute-capable Daytona routing in llm invoke path
- llm_invoke already wired with shared capability helper from US-001; this story added targeted test coverage
- Added 2 new tests: (1) backend exists but lacks execute() → fallback with SystemMessage + sandbox cleanup, (2) Daytona success path creates CompositeBackend with store routes
- Files changed: backend/tests/unit/controllers/test_llm_invoke_backend_routing.py
- All 5 Daytona routing tests pass; 7 pre-existing failures in unrelated test_search.py and TestResolveUserSettings (env leak)
- **Learnings:** `make test` requires `ENV_FILE=$HOME/.env/orchestra/.env.backend`; pytest takes ~35s for full suite; pre-existing search test failures due to Exa provider not being mocked
---

## 2026-02-13 08:14 UTC - US-004: Apply execute-capable Daytona routing in distributed worker path
- Worker path (_execute_agent_stream) already had Daytona routing with validate_daytona_execute_capability from US-001
- Added 2 new tests: (1) backend exists but lacks execute() → fallback Redis message + sandbox cleanup + done marker intact, (2) Daytona success path creates CompositeBackend with store routes and no fallback messages
- Files changed: backend/tests/unit/workers/test_tasks.py
- All 5 worker Daytona routing tests pass (3 existing + 2 new)
- **Learnings:** Worker tests use pytest fixtures (conftest.py fake_redis) unlike llm invoke tests which use unittest.IsolatedAsyncioTestCase
---

## 2026-02-13 09:20 UTC - US-005: Add cross-path tests for execute availability and command execution behavior
- Added 13 new tests in backend/tests/unit/test_daytona_cross_path.py
- TestCrossPathCapabilityConsistency (5 tests): validates shared capability checker and fallback messaging are deterministic
- TestInvokePathDaytona (2 tests): execute-capable success with CompositeBackend + execute-unavailable fallback with SystemMessage
- TestStreamPathDaytona (2 tests): execute-capable success (no SSE fallback) + execute-unavailable SSE fallback with reason
- TestWorkerPathDaytona (2 tests): execute-capable success (no Redis fallback) + execute-unavailable Redis fallback with reason
- TestCommandExecutionFlowInvoked (2 tests): proves execute-capable backend is wired as CompositeBackend.default in invoke and stream paths
- Files changed: backend/tests/unit/test_daytona_cross_path.py
- **Learnings:** Worker tests require patching at `src.agents.*` (not `src.workers.tasks.*`) for late imports; _execute_agent_stream takes params/config/files_map/todos_list/service_context/checkpointer/user_id/thread_id/stream_key/redis_client
---

## 2026-02-13 10:25 UTC - US-006: Document Daytona execute prerequisites, verification steps, and troubleshooting
- Created comprehensive docs/daytona-sandbox.md covering:
  - Package prerequisites (langchain-daytona>=0.0.2, daytona>=0.140.0) with version warnings
  - Environment variables (DAYTONA_API_KEY) and sandbox configuration
  - Architecture overview of all three execution paths (invoke, stream, worker)
  - Key source file reference table
  - Reproducible verification checklist (7 steps: deps, imports, env, unit tests, invoke, stream, worker)
  - Fallback message interpretation with reason-to-fix mapping
  - Troubleshooting matrix for common issues (wrong version, missing packages, missing API key, etc.)
  - Diagnostic shell commands for quick checks
  - Known limitations section
- Files changed: docs/daytona-sandbox.md
- All 17 Daytona-specific tests pass
- **Learnings:** docs/ directory only had index.html and slides previously; markdown docs are a new addition
---
