## Codebase Patterns
- Frontend tests: `npm run test -- --run` for CI mode, 16 test files currently passing
- Typecheck: `npx tsc --noEmit` from frontend directory
- Frontend working directory: `/home/ryaneggz/ruska-ai/orchestra/.worktrees/feat-722/frontend`
- Commits require `-s` flag for sign-off

---

## 2026-02-01 - US-001
- Installed react-big-calendar and @types/react-big-calendar
- Files changed: frontend/package.json, frontend/package-lock.json
- **Learnings for future iterations:**
  - npm install works fine from the frontend directory
  - All 16 test files pass (180 tests), 2 skipped test files are pre-existing
  - Typecheck passes cleanly with no errors
---

## 2026-02-01 - US-002
- Added ScheduleExecution, ScheduleEvent, and ScheduleWithExecutions interfaces to schedule.ts
- Files changed: frontend/src/lib/entities/schedule.ts
- **Learnings for future iterations:**
  - Existing Schedule interface has no breaking changes needed - new types extend cleanly
  - ScheduleEvent.resource.agent_id is nullable since not all schedules are agent-linked
---

## 2026-02-01 - US-003
- Added getScheduleExecutions, getRecentExecutions, getExecutionsByDateRange methods to ScheduleService
- Files changed: frontend/src/lib/services/scheduleService.ts
- **Learnings for future iterations:**
  - ScheduleService follows consistent pattern: static async methods, try/catch, console.error + rethrow
  - Execution endpoints assumed: /{id}/executions, /executions/recent, /executions with query params
---

## 2026-02-01 - US-004
- Created calendar utility functions: mapExecutionsToEvents, getCalendarDateRange, getExecutionStatusColor, filterExecutionsByPeriod
- Files changed: frontend/src/lib/utils/calendar.ts (new)
- **Learnings for future iterations:**
  - date-fns v4 is installed; use named imports (addDays, subDays, isBefore, isAfter)
  - Schedule entity has agent_id as optional field, must handle null in mapExecutionsToEvents
  - Default event duration of 30min when no completed_at timestamp exists
---

## 2026-02-01 - US-005
- Created useScheduleExecutions hook with configurable options for scheduleId, limit, autoRefresh, refreshInterval
- Returns executions, loading, error, and three fetch functions (recent, by schedule, by date range)
- Files changed: frontend/src/hooks/useScheduleExecutions.ts (new)
- **Learnings for future iterations:**
  - Hooks pattern: useCallback for fetch functions, useEffect for initial mount and auto-refresh
  - Toast notifications via `import { toast } from "sonner"` - use toast.error for failures
  - useRef for interval cleanup to avoid stale closures
---

## 2026-02-01 - US-006
- Extracted ScheduleSidebarItem component from app-sidebar.tsx into its own file
- Files changed: frontend/src/components/sidebar/ScheduleSidebarItem.tsx (new), frontend/src/components/drawers/app-sidebar.tsx (updated import)
- **Learnings for future iterations:**
  - Component was already implemented inline in app-sidebar.tsx from a previous US-006 commit; this iteration extracted it to match PRD requirement of a separate file
  - sidebar/ directory didn't exist and needed to be created
---

## 2026-02-01 - US-007
- Verified SchedulesCollapsibleGroup already implemented in app-sidebar.tsx from US-006 commit 883b8381
- All acceptance criteria met: useScheduleExecutions with limit=10, ScheduleSidebarItem rendering, View All Schedules button, loading/empty states, Calendar icon + ChevronRight
- Typecheck passes, no additional code changes needed
- Files changed: .ralph/prd.json (marked passes: true)
- **Learnings for future iterations:**
  - US-006 commit "Sidebar Navigation for Schedules" already included US-007 scope - check existing commits before implementing
  - SchedulesCollapsibleGroup is placed between ProjectsCollapsibleGroup and Threads CollapsibleGroup in the sidebar
---

## 2026-02-01 - US-008
- Removed Schedules button and unused Calendar import from SettingsPopover.tsx
- Files changed: frontend/src/components/popovers/SettingsPopover.tsx
- **Learnings for future iterations:**
  - Simple removal: delete the button JSX block and clean up unused imports
  - Typecheck confirms no other references to Calendar in this file
---

## 2026-02-01 - US-009
- Created ScheduleCalendar component using React Big Calendar with dateFnsLocalizer
- Created minimal calendar.css importing react-big-calendar base styles (full theming in US-010)
- Files changed: frontend/src/components/calendar/ScheduleCalendar.tsx (new), frontend/src/styles/calendar.css (new)
- **Learnings for future iterations:**
  - react-big-calendar exports `dateFnsLocalizer` directly; use date-fns/locale/en-US for locale
  - Calendar generic type `Calendar<ScheduleEvent>` enables typed eventPropGetter and onSelectEvent
  - CSS import `@import "react-big-calendar/lib/css/react-big-calendar.css"` works in Vite CSS files
---

## 2026-02-01 - US-010
- Created custom calendar CSS with full shadcn/ui theme overrides using CSS variables
- Styled toolbar buttons, calendar grid, headers, day cells, time view, and popup overlay
- Added .rbc-event-scheduled (blue #3b82f6), .rbc-event-success (green #10b981), .rbc-event-failure (red #ef4444) classes
- Dark mode compatibility via hsl(var(--*)) CSS variable usage throughout
- Files changed: frontend/src/styles/calendar.css (updated)
- **Learnings for future iterations:**
  - shadcn/ui CSS variables (--border, --background, --foreground, --muted, --primary, --accent, --popover, --ring) work well for theming third-party components
  - React Big Calendar uses .rbc-* class prefix for all elements
  - Dark mode works automatically since shadcn CSS variables change values in dark mode
---
