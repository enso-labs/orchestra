# Ralph Progress Log
Started: Mon Feb  9 21:07:38 MST 2026

## Codebase Patterns
- Package lives at `packages/agent-ts/` with `type: "module"` ESM setup
- Build uses plain `tsc` with bundler moduleResolution for ESM compatibility
- `.env.example` files require `git add -f` due to parent `.gitignore` pattern matching `.env*`
- `dist/` is gitignored — build artifacts are not committed
- Use `loadConfig(customEnv)` in tests instead of mutating `process.env`
- Test files live alongside source: `src/foo.test.ts` next to `src/foo.ts`
- Logger uses `process.stderr.write()` for structured JSON — spy with `vi.spyOn(process.stderr, "write").mockReturnValue(true)` in tests
- Use `any` type for `vi.spyOn(globalThis, "fetch")` to avoid complex vitest MockInstance type issues with overloaded fetch signatures
- Backend SSE format: `data: ["eventType", payload]\n\n` — event types are `metadata`, `messages`, `values`, `error`, and `[DONE]` sentinel
- Mock fetch in tests with `new Response(readableStream, { status, headers })` — use `ReadableStream` for SSE body simulation
- Subdirectories under `src/` work fine — import with `./prompts/system.js` relative paths
- Tool registry uses module-level arrays/maps — call `_resetRegistry()` in `beforeEach` for test isolation
- `ToolDefinition.parameters` is typed as `z.ZodType` — any Zod schema works (z.object, z.string, etc.)
- Tools needing external callbacks (e.g., `human_contact.promptFn`) use `options` third arg — same DI pattern as `file_writer.outputDir`
- `registerTool` replaces existing tools with the same name — supports tool override pattern
- Middleware hooks are optional — only define the hooks your middleware needs (beforeLLM, afterLLM, beforeTool, afterTool)
- `runBeforeLLM` and `runBeforeTool` chain their outputs; `runAfterLLM` is side-effect only (no chaining)
- Cannot `vi.spyOn` ESM `node:fs/promises` exports — use real filesystem I/O in tests with temp dirs and cleanup in `afterEach`
- Use factory function pattern for middleware needing dependencies (e.g., `createObservabilityMiddleware(logger)`) — allows DI and per-instance state via closures
- Tool handlers receive `options` as third arg — use `options.outputDir` for config injection in tests

---

## 2026-02-09 - US-001
- What was implemented: Project scaffolding for @ruska/agent-ts package
- Files changed:
  - packages/agent-ts/package.json (name, version, type, exports, scripts, deps)
  - packages/agent-ts/tsconfig.json (strict, ES2022, ESNext module, bundler resolution)
  - packages/agent-ts/.env.example (all 7 required env vars)
  - packages/agent-ts/src/index.ts (empty barrel file)
  - packages/agent-ts/.gitignore (node_modules, dist, .env)
- **Learnings for future iterations:**
  - Parent .gitignore blocks `.env.example` — use `git add -f` to force-add
  - `dist/` should be gitignored, not committed
  - `vitest` installed as devDep but no test files yet — `vitest run` will pass with 0 tests
---

## 2026-02-09 - US-002
- What was implemented: Zod-validated config from environment variables
- Files changed:
  - packages/agent-ts/src/config.ts (loadConfig function, Config type, Zod schema)
  - packages/agent-ts/src/config.test.ts (8 tests covering valid/invalid/defaults/coercion)
- **Learnings for future iterations:**
  - `loadConfig()` accepts an optional `env` parameter for testability — pass custom env maps in tests instead of mutating `process.env`
  - Zod `z.coerce.number()` handles string-to-number conversion for env vars like MAX_ITERATIONS
  - `dotenv.config()` is called at module import time in config.ts — tests that pass custom env bypass this
  - Use `.min(1)` on required string fields to catch empty string as missing
---

## 2026-02-09 - US-003
- What was implemented: Structured JSON logger with level filtering
- Files changed:
  - packages/agent-ts/src/logger.ts (createLogger factory, Logger interface, LogLevel type)
  - packages/agent-ts/src/logger.test.ts (11 tests: each level, suppression, metadata, JSON format, defaults)
- **Learnings for future iterations:**
  - Use `process.stderr.write()` not `console.error()` for structured JSON output — avoids console formatting
  - Spy on `process.stderr.write` with `vi.spyOn(process.stderr, "write").mockReturnValue(true)` in tests
  - Use `any` type for stderr spy to avoid complex vitest MockInstance type issues with overloaded write signatures
  - Logger has zero external dependencies — pure Node.js `process.stderr.write`
  - Export both `Logger` interface and `LogLevel` type for downstream consumers
---

## 2026-02-09 - US-004
- What was implemented: Zod schemas for ResearchResult, AgentState, and tool inputs
- Files changed:
  - packages/agent-ts/src/schemas.ts (ResearchResult, AgentState, WebSearchInput, FileWriterInput, HumanContactInput, NoteTakerInput schemas + types)
  - packages/agent-ts/src/schemas.test.ts (30 tests: valid/invalid parse, edge cases, boundary values, missing fields)
  - packages/agent-ts/src/index.ts (updated barrel exports for config, logger, and all schemas)
- **Learnings for future iterations:**
  - Use `z.infer<typeof Schema>` with same name for schema + type (TypeScript allows value/type with same name from Zod)
  - Use `.min(1)` on string fields that should not be empty (title, summary, query, filename, question, note)
  - Confidence uses `.min(0).max(1)` for 0-1 range validation
  - AgentState iteration uses `.int().nonnegative()` to reject negative and fractional values
  - Barrel file now exports all public API from config, logger, and schemas modules
---

## 2026-02-09 - US-005
- What was implemented: SSE stream client with async generator for Ruska /api/llm/stream endpoint
- Files changed:
  - packages/agent-ts/src/stream-client.ts (streamChat async generator, StreamEvent union type, StreamConnectionError class, SSE parser)
  - packages/agent-ts/src/stream-client.test.ts (16 tests: event parsing for metadata/messages/values/error/done, full session, keep-alive skip, malformed data skip, request headers/body, HTTP errors 401/500/400, network failure)
  - packages/agent-ts/src/index.ts (added streamChat, StreamConnectionError, StreamEvent, StreamChatRequest exports)
- **Learnings for future iterations:**
  - Backend SSE format is `data: ["eventType", payload]\n\n` — must parse JSON array with event type as first element
  - Use `any` type for `vi.spyOn(globalThis, "fetch")` — overloaded fetch signature causes complex MockInstance type incompatibilities
  - SSE `[DONE]` sentinel is not valid JSON — handle before `JSON.parse`
  - Use `ReadableStream<Uint8Array>` + `TextEncoder` to simulate SSE body in tests
  - `response.body.getReader()` + `TextDecoder({ stream: true })` handles chunked SSE data correctly
  - Backend sends `: keep-alive\n\n` comments — skip lines starting with `:`
---

## 2026-02-09 - US-006
- What was implemented: Prompt templates and system prompt builder with phase-based content
- Files changed:
  - packages/agent-ts/src/prompts/templates.ts (ToolDef interface, toolDescriptionBlock, researchInstructions, synthesisInstructions, outputInstructions, errorRecoveryGuidance)
  - packages/agent-ts/src/prompts/system.ts (buildSystemPrompt function, Phase type, BuildSystemPromptOptions interface)
  - packages/agent-ts/src/prompts/system.test.ts (17 tests: template fragments, phase switching, tool inclusion, composition)
  - packages/agent-ts/src/index.ts (added buildSystemPrompt, Phase, BuildSystemPromptOptions, ToolDef exports)
- **Learnings for future iterations:**
  - Subdirectories under `src/` (e.g., `src/prompts/`) work fine with bundler moduleResolution — import with `./prompts/system.js`
  - Use `ToolDef` interface (name, description, parameters) for tool definitions in prompts — decoupled from Zod schemas
  - Phase enum is `"research" | "synthesis" | "output"` — dynamic prompt middleware will use this
  - Templates are pure functions returning strings — easy to test and compose
---

## 2026-02-09 - US-007
- What was implemented: Tool registry with definitions, dispatch, registerTool, and getServerToolNames
- Files changed:
  - packages/agent-ts/src/tools/index.ts (ToolDefinition, ToolHandler types, registerTool, executeTool, getServerToolNames, getToolDefinitions, ToolNotFoundError, _resetRegistry)
  - packages/agent-ts/src/tools/index.test.ts (13 tests: registerTool add/replace/multiple, executeTool dispatch/unknown/validation/state/options, getServerToolNames filtering/empty, ToolNotFoundError shape, getToolDefinitions copy)
  - packages/agent-ts/src/index.ts (added tool registry exports)
- **Learnings for future iterations:**
  - Module-level state (arrays, Maps) requires a `_resetRegistry()` helper for test isolation with `beforeEach`
  - `ToolDefinition.parameters` typed as `z.ZodType` accepts any Zod schema — lets consumers use `z.object({})` or custom schemas
  - `registerTool` with same name replaces existing definition — useful for tool override/customization
  - `TOOL_DEFINITIONS` is a live reference to the internal array — `getToolDefinitions()` returns a copy for safety
  - `executeTool` validates with `def.parameters.parse(args)` before calling handler — Zod errors propagate to caller
- Server-delegated tools (local: false) should still have a handler that throws — prevents accidental local execution
- Use `registerWebSearch()` / `registerTavilySearch()` functions for explicit tool registration
- Tavily fallback pattern: check `process.env.TAVILY_API_KEY` and return boolean from register function
---

## 2026-02-09 - US-008
- What was implemented: web_search server-delegated tool definition and tavily_search local fallback
- Files changed:
  - packages/agent-ts/src/tools/web-search.ts (webSearchDefinition, tavilySearchDefinition, registerWebSearch, registerTavilySearch)
  - packages/agent-ts/src/tools/web-search.test.ts (16 tests: definition shape, local flags, server tool names, registration, Tavily API call mock, API error handling, combined registration)
- **Learnings for future iterations:**
  - Server-delegated tools (local: false) still need a handler registered — use one that throws descriptive error
  - `registerTavilySearch()` returns boolean indicating whether registration happened — caller can check
  - Use `vi.spyOn(globalThis, "fetch") as any` pattern for fetch mocking (from progress patterns)
  - Tavily API expects `api_key` in POST body, not as header
  - Combined registration test verifies both tools coexist with correct local/server separation
---

## 2026-02-09 - US-009
- What was implemented: file_writer local tool with path traversal protection
- Files changed:
  - packages/agent-ts/src/tools/file-writer.ts (fileWriterDefinition, fileWriterHandler, registerFileWriter)
  - packages/agent-ts/src/tools/file-writer.test.ts (12 tests: definition shape, local flag, registration, successful write, directory auto-creation, path traversal rejection x2, unicode size, default outputDir)
- **Learnings for future iterations:**
  - Cannot spy on ESM `node:fs/promises` exports — vitest can't redefine module namespace properties in ESM mode
  - For filesystem tool tests, use real temp directories with `afterEach` cleanup (`rm -rf`)
  - Path traversal protection: `resolve(dir, filename)` then check `startsWith(resolvedDir + "/")`
  - Tool handler receives `options` record — use `options.outputDir` to inject config without requiring full Config object
  - `import.meta.dirname` provides __dirname equivalent in ESM for resolving test fixture paths
- note_taker tool mutates `state.notes` directly — handler receives live state reference from executeTool
---

## 2026-02-09 - US-010
- What was implemented: note_taker local tool that accumulates research notes in AgentState
- Files changed:
  - packages/agent-ts/src/tools/note-taker.ts (noteTakerDefinition, noteTakerHandler, registerNoteTaker)
  - packages/agent-ts/src/tools/note-taker.test.ts (12 tests: definition shape, local flag, registration, note addition, category default, custom category, optional source, multi-call state mutation, ISO timestamp)
- **Learnings for future iterations:**
  - Tool handlers that mutate AgentState work because `executeTool` passes the state object by reference
  - Category defaults to "general" when not provided — use `|| "general"` pattern
  - ISO timestamps via `new Date().toISOString()` — verify in tests by re-parsing and comparing
  - No filesystem operations needed — pure in-memory state mutation is simpler to test than file I/O tools
---

## 2026-02-09 - US-011
- What was implemented: human_contact local tool with dependency-injected promptFn
- Files changed:
  - packages/agent-ts/src/tools/human-contact.ts (humanContactDefinition, humanContactHandler, registerHumanContact)
  - packages/agent-ts/src/tools/human-contact.test.ts (12 tests: definition shape, local flag, registration, promptFn call, ISO timestamp, missing promptFn error, context passthrough, error propagation)
- **Learnings for future iterations:**
  - `promptFn` is injected via `options.promptFn` (third arg to handler) — same pattern as `file_writer` uses `options.outputDir`
  - Throw a clear error when `promptFn` is not provided rather than silently failing — helps consumers understand the DI requirement
  - Test both `options: undefined` and `options: {}` cases for missing promptFn — both should throw
  - Error propagation from `promptFn` (e.g., user cancelled) passes through naturally — no need to catch/wrap
---

## 2026-02-09 - US-012
- What was implemented: Middleware system with types and runner functions
- Files changed:
  - packages/agent-ts/src/middleware/index.ts (Middleware interface, runBeforeLLM, runAfterLLM, runBeforeTool, runAfterTool)
  - packages/agent-ts/src/middleware/index.test.ts (17 tests: type shape, ordering, state propagation, arg chaining, result chaining, error passthrough, async hooks, empty arrays)
- **Learnings for future iterations:**
  - `runBeforeLLM` chains state — each middleware receives the output of the previous one
  - `runBeforeTool` chains args — each middleware transforms args for the next
  - `runAfterTool` chains result — each middleware transforms the result for the next
  - `runAfterLLM` does NOT chain — each middleware receives the same state and events (side-effect only)
  - Middleware hooks are optional — runner functions skip middlewares that don't define a given hook
  - Empty middleware array is a valid no-op — runners return original state/args/result unchanged
---

## 2026-02-09 - US-013
- What was implemented: Error handler middleware (Factor 8/9) — wraps tool errors into LLM-readable JSON
- Files changed:
  - packages/agent-ts/src/middleware/error-handler.ts (errorHandlerMiddleware with afterTool hook)
  - packages/agent-ts/src/middleware/error-handler.test.ts (9 tests: name, hook presence, passthrough, error JSON, tool name inclusion, valid JSON, reference preservation, special characters)
- **Learnings for future iterations:**
  - Error handler only needs `afterTool` hook — keep middleware focused on a single concern
  - Return `JSON.stringify({error, message, tool, suggestion})` string so LLM can parse it as structured error context
  - No-error case returns the original result by reference — no cloning needed
  - Error messages with special characters (quotes, paths) are safely handled by JSON.stringify
---

## 2026-02-09 - US-014
- What was implemented: Observability middleware (Factor 12) — logs LLM calls and tool executions with timing
- Files changed:
  - packages/agent-ts/src/middleware/observability.ts (createObservabilityMiddleware factory with all four hooks)
  - packages/agent-ts/src/middleware/observability.test.ts (13 tests: name, all hooks, LLM logging, tool logging, timing, truncation, error vs success)
  - packages/agent-ts/src/index.ts (added middleware runner exports, errorHandlerMiddleware, createObservabilityMiddleware)
- **Learnings for future iterations:**
  - Use factory function pattern (`createObservabilityMiddleware(logger)`) when middleware needs injected dependencies (Logger) — avoids module-level state
  - Track timing with closure variables (`llmStartTime`, `toolStartTimes` Map) — scoped per middleware instance
  - Use `logger.debug` for routine tool events, `logger.info` for LLM calls, `logger.warn` for tool failures — natural log level mapping
  - Truncate large args in logs with `JSON.stringify(args).slice(0, 200) + "..."` to prevent log flooding
  - Barrel file now also exports middleware runner functions (runBeforeLLM etc.) and Middleware type
---
