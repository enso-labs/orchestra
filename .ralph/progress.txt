# Ralph Progress Log
Started: Mon Feb 16 10:33:45 MST 2026

## Codebase Patterns
- `formatMessages()` in `frontend/src/lib/utils/format.ts` splits AI messages with `tool_calls` into individual `tool_input` messages with `role: "tool_input"`
- Pre-commit hooks run prettier, lint, AND full test suite — all tests must pass before commit
- When adding fields to tool_input objects, use conditional spread: `...(message.field !== undefined && { field: message.field })`
- Thread snapshot (`thread.messages`) is saved via `from_message_to_dict(messages, include_tool_calls=False)` — excludes ToolMessage, only stores Human/AI messages
- US-013 backfill depends on thread snapshot having `agent_name` — only works for threads created AFTER the feature is deployed
- React state inspection via browser: use `__reactFiber$` key on DOM elements, walk `.return` chain to find `memoizedProps`
---

## 2026-02-16 10:38 - US-010 + US-012
- **US-010**: Added `...(message.agent_name !== undefined && { agent_name: message.agent_name })` to tool_input object creation in `formatMessages()`
- **US-012**: Fixed test expectation at line 412 from `'AIMessageChunk'` to `'tool_input'` — the role was wrong in the original test
- Files changed: `frontend/src/lib/utils/format.ts`, `frontend/src/lib/utils/format.test.ts`
- **Learnings for future iterations:**
  - US-012 had to be fixed alongside US-010 because the pre-commit hook runs all tests and blocks the commit on failure
  - The `agent_name` field uses conditional spread (`!== undefined`) to differentiate between "not present" and "explicitly null"
---

## 2026-02-16 10:39 - US-011
- Added `...(response.agent_name !== undefined && { agent_name: response.agent_name })` to tool_input entry in `StreamMessageHandler.toolCall()`
- Files changed: `frontend/src/lib/utils/message.ts`
- **Learnings for future iterations:**
  - Prettier may reformat conditional spreads across multiple lines — always re-stage after prettier runs
  - The `StreamMessageHandler` class is in `frontend/src/lib/utils/message.ts`
---

## 2026-02-16 10:43 - US-013
- Backfill agent_name from thread snapshot into checkpoint messages in `search_threads` endpoint
- Builds `agent_name_map` from `thread.messages` keyed by message id, applies to checkpoint messages
- Only additive: only sets `agent_name` when missing (`"agent_name" not in msg`)
- Files changed: `backend/src/routes/v0/thread.py`
- **Learnings for future iterations:**
  - Thread snapshots reliably have `agent_name` because they go through `from_message_to_dict()` during persistence
  - Checkpoint messages may lose `lc_agent_name` during LangGraph checkpoint serialization — the backfill bridges the gap
  - The `search_threads` endpoint already enriches checkpoints with `files` and `todos` from thread snapshot — same pattern for `agent_name`
---

## 2026-02-16 10:45 - US-014
- Added `title={name}` attribute to the truncated inner `<span>` in SubagentBadge
- Files changed: `frontend/src/components/badges/SubagentBadge.tsx`
- **Learnings for future iterations:**
  - SubagentBadge truncates at `max-w-[120px]` — `title` attribute provides full name on hover
---

## 2026-02-16 11:32 - US-015
- Ran all automated checks to confirm no regressions from US-001 through US-014
- Results:
  - `backend make lint` (ruff check): All checks passed
  - `backend make format` (ruff format): 199 files unchanged
  - `frontend npm run lint` (eslint): Clean, no warnings
  - `frontend npm run build` (tsc -b + vite build): Built in 25.10s, no errors
  - `frontend npm run test` (vitest): 23 test files passed, 2 skipped, 252 tests passed, 3 skipped
- Files changed: `.ralph/prd.json`, `.ralph/progress.txt`
- **Learnings for future iterations:**
  - The 2 skipped test files (threadService.test.ts, App.test.tsx) and 3 skipped tests are pre-existing — not caused by this feature branch
  - stderr output from error-handling tests (agentService, streamSource, distributedStream) is expected — those tests verify error paths
---

## 2026-02-16 12:05 - US-016
- Browser QA verification of subagent attribution UI using agent-browser skill
- Logged in as admin@example.com, opened thread with @python-programmer subagent messages
- Verified via React fiber inspection that `agent_name: "python-programmer"` present on all tool_input, tool, and ai messages during initial session
- Confirmed "via python-programmer" renders in expanded ToolTimelineItem header (screenshot qa-13)
- Confirmed parent messages render normally without SubagentBadge (screenshot qa-17)
- Found: agent_name lost after page reload — backfill depends on thread snapshot having agent_name
- Files changed: `.ralph/prd.json`, `.ralph/progress.txt`
- **Learnings for future iterations:**
  - The US-013 backfill only works if the thread snapshot was created by a backend running the updated code
  - Pre-deployment threads will not have agent_name in their snapshot, so backfill has nothing to restore
  - React fiber inspection is useful for verifying React state: `__reactFiber$` key on DOM elements, walk `.return` to find `memoizedProps.messages`
  - agent-browser `eval` with IIFEs works better than plain expressions for complex JS
---

## 2026-02-16 12:10 - US-017
- Wrote QA results report to `.claude/plans/feat-694/05-qa-results.md`
- Report includes: automated check results, acceptance criteria matrix (9/10 PASS, 1 PARTIAL), known issue documentation, screenshot index
- Files changed: `.claude/plans/feat-694/05-qa-results.md`, `.ralph/prd.json`, `.ralph/progress.txt`
- **Learnings for future iterations:**
  - Always document both what works AND what doesn't with root cause analysis
  - Browser QA for streaming features should test both live session AND after reload
---
